# ちいさな点の、その中へ — 設計メモ

一回の連続した上スワイプで、顕微鏡ステージ上のガラススライドから、正常皮膚の毛包を
とりまく細胞の核までを連続的に潜っていくモバイルWebゲームの設計記録。

---

## 1. 素材の出典とライセンス

### 1.1 調達の判断

仕様の優先順位に従い、まず再利用可能な公的・オープンライセンスの正常H&E画像を検討した。

| 候補 | 判断 | 理由 |
|---|---|---|
| 公開WSI（whole slide image）アーカイブの正常皮膚切片 | 不採用 | 正常皮膚のWSIで再配布可能な条件（CC BY / CC0）のものを確認できなかった。多くは NC / ND 条項付き、または利用規約が「研究目的の閲覧」に限定される。本作は成果物へ画像を同梱するため、権利が明確でないものは使えない。 |
| 教育用アトラスのスクリーンショット | 不採用 | サイト規約が二次配布を許可していない。 |
| 生成AIによる病理画像 | 不採用 | 出典・権利が不明確であり、かつ仕様が禁じる「解像度が存在しない領域の架空補完」そのものになる。 |
| **オリジナルの合成H&E（本作の採用案）** | **採用** | 権利は本リポジトリに帰属。倍率ごとの別画像ではなく、単一のパラメトリックモデルから各解像度を生成できるため、仕様の「同一原図または共通座標から各解像度を生成」を厳密に満たせる。 |

> 補足：本セッションのネットワークは egress プロキシで制限されており、`microscopyu.com`、
> `histologyguide.com`、`ncbi.nlm.nih.gov`、`evidentscientific.com` への直接取得は
> ブロックされた。数値は下記 1.2 の検索結果と、光学・組織学の標準的な参照値を用いた。
> 参照できなかった一次資料に基づく数値は、その旨を「標準値」として明記している。

### 1.2 最終成果物に含まれる素材

**画像アセットはゼロ。** `dist/` に .png / .jpg / .ktx2 は一つも存在しない。
組織像はすべて `src/micro/tissueShader.ts` のGLSLモデルから実行時にGPUで生成される。
ラベル紙・バーニヤ目盛り・コンデンサー照明のテクスチャは `<canvas>` へ実行時に描画している。

| 要素 | 出所 | ライセンス |
|---|---|---|
| 組織像（全倍率） | 本リポジトリのオリジナル手続き型モデル | このリポジトリのライセンスに従う |
| スライド／顕微鏡ジオメトリ | 本リポジトリのオリジナル | 同上 |
| ラベル・目盛り・照明テクスチャ | 実行時 canvas 描画 | 同上 |
| フォント | OS標準（-apple-system 等）のみ | 同梱なし |
| 音 | WebAudio による合成のみ | 同梱なし |
| three.js r170 | npm 依存 | MIT |

### 1.3 照合した参考値

- **対物レンズの倍率カラーリング（ISO 8578）**：4x=赤、10x=黄、16–20x=緑、
  25–32x=ターコイズ、40–50x=ライトブルー、60–63x=ブライトブルー、100x以上=白。
  Nikon MicroscopyU / Nikon Instruments 用語集、および ISO 8578:2012 の記述で確認。
  根拠のない色分けは行っていない（`src/micro/optics.ts` の `ringColor`）。
- **終毛（terminal hair）の毛幹径**：直径 60 µm 超が終毛の定義。StatPearls「Histology,
  Hair and Follicle」で確認。本作は 72 µm（半径 0.036 mm）を採用。
- **毛包の区分**：infundibulum（表皮陥入部〜脂腺開口部）、isthmus（脂腺開口部〜立毛筋
  付着部）、inferior segment（成長部＋毛球）。層構造は内毛根鞘（Henle/Huxley/cuticle）を
  外毛根鞘が包み、外毛根鞘は表皮と連続。StatPearls で確認。
- **表皮／真皮の厚み**：表皮 0.05–1.5 mm、真皮 0.3–3.0 mm（部位差）。
  SIU / Buffalo の組織学教材で確認。本作は頭部皮膚として表皮 50–118 µm、
  角層 16–22 µm、真皮＋皮下 6 mm 強を採用。
- **切片厚**：ルーチンのパラフィン切片 4 µm（標準値）。
- **スライド/カバーガラス**：75 × 25 × 1.0 mm、カバーガラス 22 × 22 mm・No.1.5 = 0.17 mm（標準値）。
- **視野数（FN）22**：4x=5.50 mm、10x=2.20 mm、20x=1.10 mm、40x=0.55 mm（FN/倍率）。
- **作動距離**：4x≈18.5 mm、10x≈10.6 mm、20x≈1.2 mm、40x≈0.6 mm（乾燥系プランアクロマート標準値）。

出典リンク：

- [objective lens color codes — Nikon MicroscopyU](https://www.microscopyu.com/glossary/objective-lens-color-codes)
- [objective lens color codes — Nikon Instruments 用語集](https://www.microscope.healthcare.nikon.com/resources/glossary/objective-lens-color-codes)
- [ISO 8578:2012 — Microscopes, Marking of objectives and eyepieces](https://www.iso.org/standard/50988.html)
- [Histology, Hair and Follicle — StatPearls (NCBI Bookshelf)](https://www.ncbi.nlm.nih.gov/books/NBK532929/)
- [Histology, Skin Appendages — StatPearls (NCBI Bookshelf)](https://www.ncbi.nlm.nih.gov/books/NBK482237/)
- [Histology at SIU — skin](https://histology.siu.edu/intro/skin.htm)
- [Integument Histology Notes — University at Buffalo](https://ubwp.buffalo.edu/histology/integument-histology-notes/)
- [Skin — Histology Guide](https://histologyguide.com/slidebox/11-skin.html)

---

## 2. 対象組織と Hero Anchor

**正常皮膚（頭部）H&E切片**を採用した。仕様の第一候補どおりで、変更していない。

Hero Anchor は**一本の終毛毛包の軸上の一点**（毛隆起のすぐ下、`HERO_S = 1.30 mm`）。
表皮の毛孔を原点とする組織座標系で `(0.328, 1.259) mm`。

深さをこの位置に決めた理由：

- **4x**：円形視野の中に、毛孔・脂腺・立毛筋・毛幹・毛球までが一度に収まる。
  視野上端にわずかな素ガラスが残るだけで、切片が視野を満たす。
- **10x / 20x**：毛包と脂腺・立毛筋の位置関係が読める。
- **40x**：内毛根鞘が完成した高さなので、暗い毛幹を中心に、
  好酸性の内毛根鞘 → 明るい外毛根鞘の細胞 → 核 という同心配列がもっとも明快に読める。

副ランドマーク（倍率帯を重ねて配置し、片方が視野外へ出ても筋を見失わないようにしている）：

| ランドマーク | 組織座標 (mm) | 読める倍率帯 |
|---|---|---|
| 毛幹の中心（Hero） | (0.328, 1.259) | 全倍率 |
| 脂腺小葉 | (-0.205, 0.505) | スライド全景 〜 10x |
| 立毛筋 | (0.18,1.22)–(-1.05,0.62) | 4x 〜 40x |
| 毛包周囲の細静脈（赤血球入り） | (0.478, 1.349) | 10x 〜 40x |

病的所見は一切モデル化していない。腫瘍・出血・壊死・炎症細胞浸潤は存在しない。
描いているのは正常構造（表皮4層、乳頭層／網状層、毛包、脂腺、立毛筋、エクリン汗腺、
小血管、皮下脂肪）だけである。

---

## 3. 倍率段階と光学

`journeyProgress` 0.0 → 1.0 の写像（`src/core/journey.ts`）。
視野幅は円形視野の**直径**として定義しており、対物レンズの実視野と一致する。

| progress | 視野幅 | 対物 | NA | 分解能 0.61λ/NA | 焦点深度 | 段階 |
|---|---|---|---|---|---|---|
| 0.00 | 34.0 mm | — | — | — | — | ステージ上のスライド全景 |
| 0.08 | 20.0 mm | — | — | — | — | 機械ステージ移動 |
| 0.15 | 11.0 mm | — | — | — | — | 対物レンズ直下へ |
| 0.22 | 7.6 mm | 4x | 0.10 | 3.36 µm | 70 µm | 4x が光路へ（赤リング） |
| 0.28 | **5.50 mm** | 4x | 0.10 | 3.36 µm | 70 µm | 4x 実視野。円形視野が完成 |
| 0.45 | 3.05 mm | 4x | 0.10 | 3.36 µm | 70 µm | |
| 0.47 | **2.20 mm** | 10x | 0.25 | 1.34 µm | 11.2 µm | 10x へ切替（黄リング）＋focus rack |
| 0.63 | 1.42 mm | 10x | 0.25 | 1.34 µm | 11.2 µm | |
| 0.65 | **1.10 mm** | 20x | 0.40 | 0.84 µm | 4.2 µm | 20x へ切替（緑リング）＋focus rack |
| 0.78 | 0.72 mm | 20x | 0.40 | 0.84 µm | 4.2 µm | |
| 0.80 | **0.55 mm** | 40x | 0.65 | 0.52 µm | 1.5 µm | 40x へ切替（ライトブルー）＋focus rack |
| 1.00 | **0.275 mm** | 40x + 2× | 0.65 | 0.52 µm | 1.5 µm | 中間変倍のみ。分解能は増えない |

**最終段の考え方。** 40x のあと油浸へは行かない。代わりに 2× の中間変倍
（Optovar 相当。臨床用スタンドに実在する機構）で 0.275 mm 視野まで拡大する。
このとき表示画素は 0.25 µm/px、NA 0.65 の回折限界は 0.52 µm なので、
**最終フレームは正直に回折限界に張り付いている**。ここから先は倍率を上げても
新しい情報は出ない、という光学の事実がそのまま画に出る。DNA も細胞小器官も
発光粒子も描かない理由は、「描かない方針だから」ではなく「光学顕微鏡で見えないから」。

**焦点深度の効き方**が段階ごとに変わる点も、そのまま実装している。
切片厚 4 µm に対し、4x の焦点深度は 70 µm なので切片全体が同時にピント内。
40x では 1.5 µm しかないため、核ごとに切片内の深さが違う分だけ滲む個体差が出る。

---

## 4. マルチレゾリューションの作り方

### 4.1 「一つの原図」の意味

倍率ごとに別の絵を用意してクロスフェードする、という作り方は取っていない。
`heTissue(t, texel, optRes, focusZ, na)` という**単一の関数**が、
組織座標系のミリメートル `t` を受け取って色を返す。ピラミッドの各レベルは、
この同じ関数を、そのレベル自身のテクセルサイズと対物レンズの回折限界で
**帯域制限して**評価したものにすぎない。

したがって：

- **潜ると本当に新しい細部が出る。** 4x では核はテクセル以下なので「核密度による
  トーン」としてしか寄与しない（`paintNuclei` のループはスキップされ、平均被覆率で
  着色される）。40x では同じ核が楕円・クロマチン・核小体・核膜として解決される。
  これは引き伸ばしではなく、モデルの評価解像度が上がった結果である。
- **架空補完をしない。** 帯域制限は `w = max(texel * 0.58, optRes * 0.42)` で決まり、
  この幅より細かい特徴は平均色へ溶ける。存在しない解像度を作り出す経路がない。
- **座標が完全に一致する。** 全レベルが同じミリメートル座標を共有するので、
  レベルを跨いでもランドマークは 1 テクセルもずれない。

### 4.2 レベル構成

比 1.6 の 10 レベル。`LEVEL_COVER_MM = [18.0, 11.25, 7.03, 4.39, 2.75, 1.72, 1.07, 0.67, 0.42, 0.29]`。

- レベル0だけは切片全体を収めるため切片中心基準（3Dスライド上の標本テクスチャも兼ねる）。
  レベル1以降はすべて Hero Anchor 中心。
- テクセル数は品質段階で 1280 / 1024 / 768。
- 比を 1.6 にしたのは、画面画素サイズに対して常に等倍以上を保つため。
  比 2.0 だと各レベルの粗い側の端で 1.4 倍程度の拡大が生じる。
- 常駐は最大 5 / 4 / 3 レベル。使用中の2つ、次に必要になる1つ、保険の1つを保持し、
  それ以外は破棄する。実測 GPU テクスチャ常駐量は 6.8 MB（低品質）〜 37.5 MB（高品質）。

### 4.3 生成のタイミングとコスト

レベルは**水平ストリップに分割して複数フレームにまたがって**生成する。
指が速く動いているときは 1 フレームあたりのストリップ予算を増やす（次のレベルが
まもなく必要になるため）。生成が間に合わない場合は一段粗いレベルへ自動的に
フォールバックする。これは「画像の欠落」ではなく「対物レンズがまだ落ち着いていない」
ように見えるので、破綻にならない。

**KTX2 / 圧縮テクスチャは採用していない。** 実行時生成なのでダウンロードも
デコードも存在せず、圧縮の主目的（転送量とデコード時間）が発生しない。
GPU 常駐量は上記のとおりレベル数の制限で押さえている。
（もし将来ベイク済みアセットへ切り替えるなら、そこで初めて KTX2 が意味を持つ。）

---

## 5. 3D と 2.5D の境界

仕様の絶対条件——H&E切片を厚い3D細胞の洞窟へ変換しない——を、次の分担で守っている。

| 対象 | 表現 |
|---|---|
| 顕微鏡ステージ、スライド、カバーガラス、ラベル、レボルバー、対物レンズ、コンデンサー | **実寸の3Dジオメトリ**（three.js） |
| 標本（低倍率〜高倍率） | **マルチレゾリューション2D**。スライド面に貼られた薄い平面 |
| 切片の厚み・皺・凹凸 | ごく控えめ。折れ 1 本（コントラスト 7 %）、ナイフチャター（1.4 %）、厚みむら（4.5 %）のみ |
| 核 | 切片内の楕円断面。3D球体として空間に浮かべない |
| 細胞内部 | 侵入しない。最終段は細胞レベルで止まる |

別世界級の変化は、尺度・視野・H&E色彩・組織構築・核密度・焦点深度・円形視野・
顕微鏡光だけで作っている。ネオン、発光縁、透明ゼリー、映画的な過剰ボケは使っていない。

**円形視野**は黒いPNG枠ではない。`src/optics/microscopeView.ts` で、
- 視野絞りのエッジ（約1.2px幅の光学的な境界）
- cos⁴ に近い周辺減光（最大16 %、控えめ）
- 視野最外周のみのごく僅かな倍率色収差（R/B で 1.2 % / 1.0 %）
- 対物レンズが降りてくる過程で 0.10 → 0.485（短辺比）へ成長する開き方

として計算している。円が完成するまでの間、円の**外側は透明**なので、
背後の3Dハードウェアがそのまま見える。暗転もカットもない。

---

## 6. 同一性の維持

- 3Dパスではカメラが常に Hero Anchor を注視し、`setViewOffset` で錐台をずらして
  アンカー位置（縦持ち画面上端から41.5 %、横持ち45.5 %）へ固定する。
- 顕微鏡パスでは視野中心が Hero Anchor そのもの。
- **両者は同じ視野幅の数値を使う。** 3Dカメラの距離は
  `distanceFor(fieldMM, elevation)` として視野幅から逆算しており、手で調整した
  値ではない。だから対物レンズをくぐる瞬間に尺度が飛ばない。
- 切片は 5.4° 傾けてマウントされている（実物がそうであるように）。この回転は
  顕微鏡パスにも `uRot` として同じ値で適用されるので、**位置だけでなく向きも**
  倍率をまたいで保存される。

自動計測の結果は §8 のとおり、全倍率・全端末でランドマークのずれは 0.0000。

---

## 7. 入力

- Pointer Events + `setPointerCapture` + `pointercancel` / `lostpointercapture`。
- `touch-action: none`（CSS）、`overscroll-behavior: none`、`user-select: none`、
  `-webkit-touch-callout: none`、`viewport-fit=cover`、`maximum-scale=1`。
- **速度ではなく移動量**で倍率を制御。`dp = -dy * correction / travelPx`。
  `travelPx = clamp(0.92 * innerHeight, 320, 760)` CSS px。
- **斜め補正**：ストローク方向を指数平滑した単位ベクトルの縦成分 `|axis.y|` から
  `correction = min(1/max(|axis.y|, 0.62), 1.62)` を掛ける。斜め45°のストロークは
  同じ長さの垂直ストロークと同じだけ進み、ほぼ水平なワイプはほとんど進まない。
- 指を離しても進捗は保持されるので、一回のスワイプでも複数回に分けても到達できる。
- ゆっくり動かせば倍率境界を何度でも往復できる（時間駆動の要素が一つもないため）。
- 速く動かしてもランドマークは画面上の同じ点にあるので見失わない。
- 制限時間・採点・誤答なし。
- **操作領域は画面全体**。44 pt どころか、外せる的が存在しない。
- **指遮蔽対策**：ランドマークのアンカーを画面中央より上へ寄せている
  （縦 41.5 %、横 45.5 %）。親指の弧は視野の下に来る。
- **3秒迷ったときのヒント**：矢印も文章も出さない。機械ステージが標本を対物レンズ側へ
  わずかに持ち上げて戻す（`FocusController.nudge`）。マクロ段階ではステージが数 mm
  だけ横に動いて戻る。reduced motion では振幅を半分にする。

---

## 8. テスト結果

`node tools/capture.mjs <url> <out> [--devices=…] [--quality=…]` で自動取得。

### 8.1 撮影地点とランドマーク一致（iPhone 縦、high）

`landmarkDelta` は「ランドマークの実測スクリーン位置 − 目標アンカー位置」（0〜1正規化）。

| progress | 地点 | 視野 | 対物 | 使用レベル | 常駐 | landmarkDelta |
|---|---|---|---|---|---|---|
| 0.00 | スライド全景 | 34.000 mm | — | 0→1 @0.00 | 25 MB | (0.0000, −0.0000) |
| 0.14 | 対物直前 | 11.981 mm | — | 0→1 @0.00 | 25 MB | (−0.0000, 0.0000) |
| 0.23 | 円形視野へ越境中 | 7.201 mm | 4x | 0→1 @0.90 | 25 MB | (−0.0000, −0.0000) |
| 0.34 | 4x | 4.467 mm | 4x | 1→2 @0.91 | 25 MB | (0.0000, 0.0000) |
| 0.54 | 10x | 1.817 mm | 10x | 3→4 @0.83 | 31.3 MB | (0.0000, 0.0000) |
| 0.71 | 20x | 0.905 mm | 20x | 5→6 @0.31 | 31.3 MB | (0.0000, 0.0000) |
| 0.86 | 40x | 0.468 mm | 40x | 6→7 @0.71 | 31.3 MB | (0.0000, 0.0000) |
| 1.00 | 細胞レベル | 0.275 mm | 40x | 7→8 @0.85 | 31.3 MB | (0.0000, 0.0000) |

### 8.2 画面サイズ・向き

| 端末プロファイル | 論理サイズ | 結果 |
|---|---|---|
| iPhone 縦 | 390 × 844 | 全地点 OK、ずれ 0 |
| iPhone 横 | 844 × 390 | 全地点 OK、ずれ 0 |
| iPhone Pro Max 縦 | 430 × 932 | 全地点 OK、ずれ 0 |
| iPad 縦 | 834 × 1194 | 全地点 OK、ずれ 0 |
| iPad 横 | 1194 × 834 | 全地点 OK、ずれ 0 |
| iPhone 縦 + reduced motion | 390 × 844 | 全地点 OK、ずれ 0 |

### 8.3 品質段階

| 段階 | テクセル | SS | 常駐上限 | 実測常駐 |
|---|---|---|---|---|
| low | 768 | 1 | 3 | 6.8 – 11.3 MB |
| medium | 1024 | 2 | 4 | 16 – 20 MB |
| high | 1280 | 2 | 5 | 25 – 37.5 MB |

### 8.4 fresh-context verifier（画像のみを渡す判定）

仕様に従い、コードを一切見せず、8枚の連番PNGだけを渡した検証を2回行った。
検証者には「1回の連続操作の連続フレーム」という以上の情報を与えていない。

**1回目で指摘され、実際に直した問題：**

| 指摘 | 対応 |
|---|---|
| 越境フレームが二重露光に見える。3D側が円の内側へ透けている | 円の内側を完全不透明にした。円は「アパーチャ」であって「クロスフェード」ではない |
| F2 と F4 で毛幹の角度が変わっており、溶暗の中に説明のつかない回転がある | 顕微鏡側の回転をカメラ投影から実測する方式へ変更。3Dが見せていた向きをそのまま引き継ぐ |
| 視野内に浮かぶ暗い円環がレンズの欠陥に見える | マーカー環の半径を 3.2 mm へ拡大。4x の視野の外側になり、ハードウェアが見えている間だけ見える |
| 4x の真皮に核がほぼ無く、10x で急に湧く | 線維芽細胞の密度と濃度を上げた |
| 高倍率の真皮が「拡大されたぼけ」で、核だけがくっきりしている | 膠原線維に中間スケールの層を追加 |
| 毛幹が一様な茶色ノイズ。キューティクルも髄質も無い | 皮質細胞、キューティクル鱗片（輪郭自体を段差にする）、髄質を追加 |
| 核がスタンプの複製に見える | サイズ・染色濃度・クロマチンの粗さを核ごとに大きく振り、核小体の出現率を上げ、隣接核が重なるようにした |
| 赤血球が均一な粒 | 中心部の淡明（両凹円板）とサイズ差を追加 |
| ステージ目盛りの数字が鏡像化していて読めない | 数字を廃止し、目盛り線のみに |

**2回目で指摘され、実際に直した問題：**

2回目の検証者はピクセル値を直接測って判定した（高周波エネルギーの計測）。

| 指摘（計測値つき） | 対応 |
|---|---|
| 真皮の高周波エネルギーが 4x→20x で 6.78 → 2.41 へ 65 % 低下し、40x で 6.49 へ跳ね戻る。中倍率で「同じ模様を大きくしただけ」になり、高倍率で新しい階層が突然点く | 膠原線維のオクターブの穴を埋めた（束 17 µm / 亜束 / 線維 / 細線維 / 微細線維の5層。各層は `legible()` で解像可能になった時点で加算される） |
| 40x の中心部の高周波エネルギー（5.02）が 4x（7.97）より低い。倍率17倍なのに情報が減っている | 毛幹に皮質細胞・キューティクル・髄質を追加。輪郭も鱗片で段差をつけた |
| 顕微鏡フレーム5枚すべてに被写界深度が無い。隅から隅まで均一に鮮鋭で、ピント合わせという体験が完全に欠けている | 切片内の深さ場を導入。膠原線維も核も、焦点面からの距離に応じて滲む。40x では同一視野内に鮮鋭な核と滲んだ核が共存する |
| 越境時の周囲が輝度 95–97 の平坦な灰色に洗われる。遮蔽ではなくアルファ合成に見える | 周囲の減光開始を 0.235 へ遅らせた。作業台は明るいまま残り、その上に不透明な円が開く |
| 視野絞りの縁に減光が一切ない | 外周 10 % に追加の減光を入れた |
| 消音ボタンの背景が黒地で消える | 背景を明度側へ変更 |

**2回目で「良い」と確認された点：**

- 8枚が一つの場所の連続拡大として読める。毛幹の幅が 8 → 12 → 30 → 62 → 118 → 201 px と単調に増加し、飛びがない。
- 越境フレームで、円の内側と外側の表皮線が同じ y（568–574 px）に載っている。
  同一レンダーの一部であり、二つの映像の溶暗ではない。
- ステージの円弧が円の内側へ一度も侵入していない。
- 尺度が物理的に正しい。最終フレームの視野 ≈ 282 µm、核 10–13 µm、
  4x の視野 ≈ 4.7 mm、毛包の深さ 3.6 mm。いずれも実物の範囲。
- SF的な細胞の洞窟・発光粒子・立体化した核は皆無。

### 8.5 実際の指操作（デバッグAPIを介さない）

`node tools/qa.mjs <url>` は、`__zoom.setProgress` ではなく本物のポインタドラッグを流す。

| 検査 | 結果 |
|---|---|
| 一回の連続上スワイプで細胞レベルへ到達 | PASS（progress = 1.0000、40x、視野 0.277 mm） |
| そのまま下へ戻すとスライド全景へ | PASS（progress = 0.0000） |
| 指を離しても進捗が保たれる | PASS（0.4868 → 0.4868） |
| 二回目のストロークは続きから | PASS（0.4868 → 0.9737） |
| 24°の斜めストロークが主軸へ補正される | PASS（斜め/垂直 = 1.089） |
| ほぼ水平なワイプでは潜航しない | PASS（0.500 → 0.509） |
| pointercancel の後も入力が生きている | PASS |
| 画面上に指示テキスト・矢印が存在しない | PASS（可視テキスト長 0） |
| 潜航中の画面回転で進捗・視野・ランドマークが保たれる | PASS（視野一致、ずれ 0.00000） |
| セッション全体でコンソールエラー 0 | PASS |

### 8.6 その他

- **型検査**：`tsc --noEmit` エラー 0（`strict`, `noUnusedLocals`, `noUnusedParameters`）。
- **ビルド**：成功。`dist` 合計 604 KB（gzip 162 KB）。**画像アセット 0 個**。
- **コンソール**：全端末・全地点で error 0、requestfailed 0。
- **画像ロード失敗**：ロードする画像が存在しないため該当なし。
  WebGL2 が取れない場合は日本語のフォールバック画面を出す。
- **フレームペーシング**：この環境の Chromium は SwiftShader（CPUラスタライザ）で動く。
  重いフラグメントシェーダーはまさにCPUが最も苦手とするものなので、ここで得られる
  ミリ秒はリグレッション検出の目安であって実機GPUの予測ではない。
  **実機での駒送りは本環境では測定できていない**（既知の制約）。
  代わりに、レベル生成を1フレームあたり小さなストリップ（高品質で1/8レベル）に
  分割し、生成が間に合わない場合は一段粗いレベルへ自動フォールバックする設計にして、
  生成コストがフレームを止めない構造にしてある。
- **10往復**：0 → 0.25 → 0.5 → 0.75 → 1.0 → …を10周。
  常駐量 31.3 MB → 37.5 MB（高品質、上限5レベル内で収束）。リークなし。
  10周とも 40x でのランドマーク位置は同一。
- **回転**：`orientationchange` と `visualViewport.resize` を購読し、
  アンカー位置・fov・カメラ方位を縦横で切り替える。

---

## 9. 分かったこと（開発中に潰した実バグ）

記録として残す。どれも画面を見なければ気づけなかった類のもの。

1. **`renderer.setViewport` / `setScissor` は devicePixelRatio を掛ける。**
   ピラミッドのレンダーターゲットへこれを使っていたため、Retina 端末では
   各レベルが自分のテクスチャの左下 1/4 にしか描かれていなかった。
   結果、40x で毛包ではなく無関係な真皮が中心に来ていた。
   レンダーターゲット自身の `viewport` / `scissor` / `scissorTest` を使うのが正解。
2. **正規化スクリーン座標を回転してから非等方スケールを掛けていた。**
   縦持ちでは縦横比 2.16 なので、5.4° の切片回転が強烈なせん断になっていた。
   ミリメートルへ変換してから回転する。
3. **GLSL の `pow(x, 2.0)` は x が負だと未定義。** 毛球とシワのガウシアンで
   これをやっており、真皮に矩形のアーティファクトが出ていた。`x*x` に置換。
4. **`step()` で組織の連続量をゲートしていた。** 毛包周囲の膠原線維の向きを
   `step(s, FOL_LEN+0.2)` で切っており、真皮に定規で引いたような直線が出ていた。
5. **ボロノイの格子間隔を半径に応じて変化させていた。** 脂腺小葉の内部が
   等高線（指紋）状になった。格子は一定にして、見た目の変化は色で付ける。
6. **three.js の組み込みマテリアルは出力カラースペース変換を掛ける。**
   スライド上の標本にだけこれが効いて H&E がパステルに褪せ、円形視野の中と
   外で同じ組織の色が食い違っていた。素通しの ShaderMaterial に置換。
7. **皮下脂肪が素ガラスより明るかった。** 低倍率でスライドに穴が空いたように見えた。

---

## 10. 既知の制約

- **合成組織である。** 実物のH&E標本を参照して構築したが、実写ではない。
  診断学習用途には使えない。本作は診断ゲームではないので設計上の欠陥ではないが、
  誤解を避けるために明記する。
- **切片は一枚、視野は一箇所。** ステージを自由に動かして別の毛包を探すことはできない。
  潜航の軸は Hero Anchor に固定されている。
- **焦点は screen-space。** ピラミッドのレベルは切片中央（focusZ = 0）で焼いてあり、
  focus rack は合成側のぼかしとして掛ける。核ごとの深さ差による滲みは焼き込み済みだが、
  「ノブを回すと別の深さの核が浮かび上がる」ところまでは再現していない。
  レベルを焦点面ごとに焼き直すのは、モバイルのフレーム予算に合わない。
- **WebGPU は使っていない。** WebGL 2 のみで全機能が成立する。
  WebGPU 経路を足せば生成をコンピュートシェーダーへ移せるが、
  現状 WebGL 2 で 60 fps を割らないため必要性がない。
- **音は合成音のみ。** 実機の Zeiss / Olympus の音を録ったものではない。
- **低スペック端末での初回生成。** 起動時にレベル0と3を同期生成するため、
  非常に古い端末では初回表示までに数百 ms かかりうる。
  その間はランプが暖まるアニメーションを出している。
- **色覚。** H&E の青紫／ピンクの対比は第2色覚異常でも保たれるが、検証はしていない。

---

## 11. ファイル構成

```
src/
  main.ts                     ループ、合成、品質の自動降格
  styles.css                  touch-action / safe-area / reduced-motion
  core/
    journey.ts                JourneyProgress：進捗 → 視野・対物・段階
    input.ts                  Pointer Events、移動量駆動、斜め補正、idle検出
    audio.ts                  WebAudio 合成（ランプ、ラック、レボルバーのクリック）
    quality.ts                品質段階の検出と設定
  micro/
    specimen.ts               共通座標系。スライド寸法、毛包幾何、Hero Anchor
    optics.ts                 対物レンズ諸元、FN、分解能、焦点深度、ISO色リング
    tissueShader.ts           単一パラメトリックH&Eモデル（GLSL）
    tissueMaterial.ts         モデル → 1ドローコール（回転格子スーパーサンプル）
    tissuePyramid.ts          MultiresolutionTissuePyramid：常駐管理・段階生成・ブレンド
  optics/
    microscopeView.ts         CircularMicroscopeMask ＋ 明視野合成
    focusController.ts        FocusController ＋ ObjectiveTransitionController
  scene/
    physicalSlideScene.ts     PhysicalSlideScene：実寸の3Dハードウェア
    orientationCamera.ts      OrientationSpecificCamera ＋ LandmarkTracker
  debug/
    debugApi.ts               DebugProgressAPI（window.__zoom）
  preview.ts                  開発専用：モデルのコンタクトシート
tools/
  capture.mjs                 全端末・全地点の自動撮影とランドマーク検証
  shot.mjs                    単発スクリーンショット
  dev/                        開発中に使った診断スクリプト
```

## 12. 使い方

```
npm install
npm run dev          # http://localhost:5173/
npm run build        # 型検査 + 本番ビルド
npm run capture      # 全端末の撮影とレポート（要 dev/preview サーバー）
```

デバッグ：

- `?debug=1` — 進捗・視野・対物・NA・分解能・焦点深度・使用レベル・常駐量・
  ランドマーク実測位置・fps のオーバーレイと、目標／実測アンカーの十字線。
- `?p=0.86` — 指定した進捗で起動。
- `?quality=low|medium|high` — 品質段階の固定。
- `?mark=1` — 生成される全レベルへ絶対ミリメートル格子と Hero Anchor の環を焼く。
- `?nopatch=1` / `?nospec=1` — スライド上の標本テクスチャの内訳を切り分ける開発用。
- `window.__zoom` — `setProgress` / `getState` / `landmarkScreen` / `waitReady` /
  `memory` / `capturePoints` / `setQuality`。
- `window.__zoomUniforms()` — 合成シェーダーの実効ユニフォーム値。
- `window.__zoomDumpLevel(i)` — ピラミッドのレベル i を PNG data URL として読み戻す。
- `/preview.html?set=all|focus` — 組織モデルだけを倍率別に並べた開発用ページ。
