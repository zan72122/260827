# PROTOCOL.md — 原資料の条件・教材設定・未検証事項の区別

このファイルは、ゲーム内のモデルが **どこまで原資料に基づき、どこからが本教材の設定か** を
明確に分けて記録するためのものです。実装値は `src/sim/protocol.ts` の `BATHS[].ref`（原資料）と
`TEACHING`（教材係数）に対応しています。

---

## 0. 参照の取得方法（重要）

この実装を行った環境では、外向きの HTTPS が組織のエグレスポリシーで制限されており、
以下のホストへ**直接アクセスできませんでした**（`CONNECT tunnel failed, response 403`）。

- `www.newcomersupply.com`（S1）
- `www.leicabiosystems.com`（S2, S3）
- `commons.wikimedia.org` / `upload.wikimedia.org`（S4）
- `www.marienfeld-superior.com`（S5, S6, S7）
- `developer.mozilla.org`, `threejs.org`, `scikit-image.org`（S9, S8）

そのため S1・S3・S7 の内容確認は **Web 検索経由で得られた各ページの記述**に拠っています。
原文 HTML を直接取得して逐語照合したわけではありません。この点は未解決の制約として記録します。
S2 と S5・S6 は、ブリーフ本文に記載された数値（槽の外寸、スライド寸法）と検索結果を突き合わせました。

---

## 1. [S1] の手順（原資料の条件）

出典: Newcomer Supply, *Hematoxylin Stain, Harris Modified* — 「H&E STAINING PROCEDURE WITH HARRIS MODIFIED」
<https://www.newcomersupply.com/product/hematoxylin-stain-harris-modified/>

選択肢のうち、本教材は **色出し = Scott Tap Water Substitute**、**対比染色 = Eosin Y Working Solution** を採用します。

| # | 工程 | 原資料の条件 | 秒数の規定 |
|---|------|--------------|-----------|
| 1 | キシレン 3槽 | 各 3 分 | あり (180 s) |
| 2 | 100% エタノール 2槽 | 各 10 dips | 回数のみ |
| 3 | 95% エタノール 2槽 | 各 10 dips | 回数のみ |
| 4 | 蒸留水 | 「十分に洗う」 | **なし** |
| 5 | ヘマトキシリン Harris Modified | 1〜5 分（核染色の濃さの好みによる） | 範囲のみ |
| 6 | 水道水 | 3 回交換して十分に洗う | **なし**（回数のみ） |
| 7 | 1% 酸アルコール | 「速やかに」分別 | **なし** |
| 8 | 水道水 | 3 回交換して十分に洗う | **なし**（回数のみ） |
| 9 | Scott 水道水代用液 | 10 dips | 回数のみ |
| 10 | 水道水 3回交換 → 蒸留水ですすぐ | — | **なし** |
| 11 | 余分な水を切り、70% エタノール | 10 dips | 回数のみ |
| 12 | Eosin Y Working Solution | 30 秒〜3 分（濃さの好みによる） | 範囲のみ |
| 13 | 95% エタノール 2槽 | 各 1 分 | あり (60 s) |
| 14 | 100% エタノール 2槽 | 各 10 dips | 回数のみ |
| 15 | キシレン 3槽 | 各 10 dips | 回数のみ |
| 16 | 適合する封入剤とカバーガラスで封入 | 量・角度・速度の規定なし | **なし** |

**秒数を規定していない箇所に、臨床上の正解秒数は設定していません。**
ゲーム内の「原手順の条件」表示にも、原資料の表現（「速やかに」「十分に」）をそのまま出しています。

## 2. 本教材が選んだ設定値（原資料の範囲内での選択）

これらは S1 が幅を持って示す範囲の中から、5〜10 分で 1 プレイが終わるように選んだ**教材設定**です。
唯一の臨床的正解ではありません。

| 項目 | 値 | 根拠 |
|------|----|------|
| 核染色の基準時間 | 180 秒 | S1 の「1〜5 分」の中央付近 |
| エオジンの基準時間 | 60 秒 | S1 の「30 秒〜3 分」の中で短め |
| 分別の目安 | 5 ディップ前後 | S1 は「速やかに」のみ。ゲーム内で操作可能にするための枠 |

## 3. 教材用モデルの係数（原資料に無い値）

`src/sim/protocol.ts` の `TEACHING` に集約しています。
**因果の向き**（何が何を増減させるか）は S2 / S3 の記述に沿わせていますが、
**速度定数・上限値・しきい値は本教材が調整した値**であり、実試薬の反応速度を検証したものではありません。

| 係数 | 値 | 意味 |
|------|----|------|
| `tickSec` | 0.05 s | 化学モデルの固定タイムステップ。ログの再生単位 |
| `gridW` × `gridH` | 20 × 14 | 切片を覆う状態マップの解像度 |
| `kDewaxXylene` | 0.017 /s | キシレン中のパラフィン溶解（1次） |
| `kDewaxEtoh100` | 0.0006 /s | 100% エタノールのわずかな脱パラフィン |
| `kPolar` | 0.35 /s | 切片内の媒体の極性が槽の値へ近づく速さ |
| `kWater` / `kWaterInXylene` | 0.06 / 0.004 /s | 残留水分の交換速度 |
| `kClear` | 0.4 /s | 透徹（キシレン置換）の進行 |
| `kHemaNuc` / `hemaNucMax` | 0.0075 /s / 1.35 | 核ヘマトキシリンの結合 |
| `kHemaBg` / `hemaBgMax` | 0.02 /s / 0.55 | 核外ヘマトキシリンの結合（速く飽和する＝分別が要る） |
| `kDiffBg` / `kDiffNuc` | 0.5 / 0.04 /s | 酸アルコールによる脱色（背景が速く、核は遅い） |
| `bluedProtection` | 0.7 | 色出し後は酸で抜けにくくなる度合い |
| `kBlue` | 0.85 /s | Scott 液での色出しの進行 |
| `kEosin` / `eosinMax` | 0.02 /s / 1.3 | エオジンの取り込み |
| `kEosinLoss*` | 水 0.02、70% 0.004、95% 0.0012、100% 0.0004 /s | エオジンの溶出 |
| `filmBase` / `filmSpeedGain` | 0.010 / 0.9 | 液膜の基準量と、引き上げ速度による増加 |
| `kDrain` / `filmDrainFloor` | 0.55 /s / 0.25 | 液切りの速さと残る膜の下限 |
| `kEvap` / `kDry` / `dryGraceSec` | 0.03 /s / 0.02 /s / 12 s | 蒸発と乾燥障害。通常の移動（数秒）では罰しない |
| `boundaryMl` / `kDisperse` | 3 mL / 0.12 /s | スライド近傍の境界層の体積と拡散 |
| `volumeMl`（各槽） | 250 mL | 槽全体の容量。1 枚の持ち込みで失活しない大きさ |

### 時間の扱い

- **操作時間**（指の動き、ラック速度、滴下、カバーガラス）は常に等倍です。
- **教材内モデル時間**は、ラックを静かに沈めたまま 1 秒以上待っているときだけ、最大 ×10 まで滑らかに加速します。
  - 加速はディップ（往復）中は働かないため、**ディップ回数が加速で水増しされることはありません**。
  - **分別（酸アルコール）は加速しません。** ただし反射神経を測らないよう、目安に達しても自動では引き上げません。引き上げるのは常にプレイヤーです。
- 画面には「教材内経過」と「実操作」を分けて表示します。原資料にない秒数を実時間として提示することはありません。
- 説明画面を開いている間、タブが非表示の間、画面回転の処理中は時計を止めます。1 フレームで進む時間は 0.1 秒に制限しているので、復帰時に時間が飛びません。

### 「水道水 3 回交換」のモデル化

S1 は回数だけを指定しています。本教材では、スライド近傍の**境界層**（`boundaryMl` = 3 mL、教材係数）
という近似を置き、引き上げ時にできる新しい液膜はこの境界層の液からできるものとしました。
交換すると境界層が新しい水に置き換わり、交換しなければ前の液の成分が残ります。

ただし 250 mL の槽に対して液膜は約 0.14 mL しかないため、
**1 回の持ち込みで槽全体が失活するような誇張はしていません**。
その結果、「3 回交換しなかった」ことは像に強く出ないことがあります。
振り返りではこれを「手順からの逸脱だが今回の像には強く出ていない」として、
像に出た所見とは分けて表示します。像に出ない逸脱を、像に出たかのようには説明しません。

## 4. 因果の向きと、その根拠

| 因果 | 根拠 |
|------|------|
| 脱パラフィン不足 → その領域の染色不足・不均一 | [S3] 「キシレンでの時間が足りないと、パラフィンに覆われた部分が染まらない／むらになる」 |
| 分別が強い・長い → 核が淡くなる | [S3] 「分別液が強すぎる、あるいは適正な分別液でも時間が長すぎるとヘマトキシリンが抜けて核が淡くなる」 |
| 分別不足 → 背景に色素が残る | 退行性染色の一般的な機序（[S2]） |
| 色出しは分別とは別 | Scott 液は色調を変える工程。本モデルでも色素量ではなく**色ベクトルの向き**だけを変える |
| 水はエオジンを溶かし出す | [S3] 「水はエオジンの分別に使われる」 |
| 溶剤への水の混入 → 脱パラフィン・透徹の阻害 | [S3] 「乾燥が不十分だと水がキシレンのパラフィンへの到達を妨げる」 |
| 残留水分 → 透徹不良 → 曇り | 上と同じ機序。本モデルでは水分が多いとキシレン置換が進まない |
| 封入剤量・カバーガラスの接触経路 → 未充填・気泡 | 幾何の帰結（下記 5.） |

**未検証**: 上の「向き」は文献に支持されますが、
どのくらいの時間でどれだけ変化するかという**強度と時定数は本教材の調整値**です。
また、薄い核・薄いエオジン・むらはいずれも複数の原因から生じ得るようにしてあり、
1 つの所見を 1 つの原因に固定していません。

## 5. 封入の幾何モデル

`src/sim/mounting.ts`。次の 3 つの機構だけを再現しています。

1. カバーガラスを角度 θ で降ろすと、接触辺から順に隙間が最終膜厚まで閉じる（閉じる位置 `u = gap / (L·sinθ)`）。
2. 封入剤の濡れ広がり前線は、その場の隙間に比例した速さで進む。
3. 前線が届く前に隙間が閉じた場所に空気が残る（気泡）。封入剤が足りなければ前線が止まり未充填になる。

- 最終膜厚 `gap = clamp(V/(L·W), 0.010, 0.045) mm`、前線速度係数 900 /s、上限 60 mm/s は**教材係数**です。
- 24×50 mm を厚さ約 0.02 mm で満たすのに必要な量は約 24 µL という**計算値**を目安にしています（実務の推奨量ではありません）。

## 6. 顕微鏡画像のモデル

- 色分離は [S8] と同じ Ruifrok & Johnston の H&E ベクトルを使います。
- **実写画像がある場合**: 分離に使った行列でそのまま再合成するため、状態が基準値に一致すれば元画像の色調を再現します。
  これは「元画像の色調が唯一の正解」という主張ではなく、比較の基準点として置いた取り決めです。
- **模式図で代替する場合**: 表示用のエオジンベクトル `[0.164, 0.812, 0.560]` と中性の残差ベクトルを使います（教材係数）。
- 色出し前のヘマラムの色ベクトル `[0.32, 0.60, 0.73]` は、色出しが色調を変えることを示すための**教材係数**であり、実測の吸収スペクトルではありません。
- 気泡・未充填・曇りは組織のマップを書き換えず、**光学的な観察妨害としてのみ**重ねます。弱い染色でも核の位置は動きません。
- 完成画像の上下は、ラックに挿したときのスライドの向きに合わせています（教材上の取り決め）。実際の顕微鏡では標本の載せ方で向きは変わります。
- 倍率・µm スケール・whole-slide imaging 由来といった表示はしていません。

## 7. 資料間の差異（記録）

- **カバーガラスの厚み**: 本教材のモデルは 0.17 mm ですが、[S7] の "Cover glasses thickness No. 1" は **0.13〜0.16 mm** です。
  0.17 mm は No. 1.5（一般に 0.16〜0.19 mm）に相当します。都合よく片方に寄せず、差異として記録します。
- **ラックの枚数**: [S5] のラックは最大 10 枚用ですが、本教材では 1 枚だけ装着し、他のスロットは空のままにしています。
- **染色槽の外寸**: 蓋込み 105 × 85 × 70 mm を [S5] の値として採用し、内寸・液面高さ（55 mm）・
  ラックの線径などは実測値が確認できないため教材モデルの寸法としています。

## 8. この教材が主張しないこと

- 実作業の力量認定を行う教材ではありません。
- 臨床用の染色品質予測器ではありません。反応速度は検証されていません。
- 完成画像は「実写組織を基にした染色状態の教育用シミュレーション」であり、
  各条件で実際に染めた対照標本の写真ではありません。
- 医学的妥当性について専門家のレビューを受けていません。
