# Reifendrehen — 検証レポート / verification report

> 実装物: 木の輪から子ヒツジの粗形を一体だけ切り出す3D Webゲーム（iPhone / iPad、一指操作）

## 1. 起動方法

```bash
npm install
npm run dev        # http://localhost:5173   （--host 済み。同じLANのiPhone/iPadから開ける）
# もしくは
npm run build && npm run preview     # http://localhost:4173
```

WebGL2 のみ。ログイン・課金・広告・外部API・WebGPU は使用しない。

開発用フラグ（通常プレイでは出ない）:

| フラグ | 内容 |
| --- | --- |
| `?plain=1` | 工房なし。輪と木片だけの無地環境 |
| `?debug=1` | 数値表示＋断面輪郭・切断境界・木片の元位置のワイヤーフレーム |
| `?orbit=1` | 自由カメラ（側面・背面の点検用） |
| `?auto=1` | タイトルを飛ばす |

## 2. 形状の同一性をどう保証したか

形の定義はプロジェクト全体で **1つだけ**。`src/core/profile.ts` の閉じた2D多角形
（子ヒツジの横姿）である。`src/core/sector.ts` はこの多角形を2つの境界面のあいだで
軸まわりに回して立体を作る。

```
輪材   = sector(theta1 .. theta0 + 2pi)     支持具に固定されたまま残る
木片   = sector(theta0 .. theta1)           子どもが取り出す一片
```

境界面は「軸を含む半平面」で、任意で鋸の刃厚の半分だけ横にずらせる。さらに
「ある半径より外側だけずらす」ことができ、これが**切りかけの状態そのもの**になる。
第2のモデルもモーフも登場しない。取り出した木片は、輪の中にあったときと
**同じ頂点配列のまま**である。

寸法（本作の設計値。資料の実測値ではない）:

| | |
| --- | --- |
| 輪の外径 | 410 mm |
| 中心穴の径 | 210 mm |
| 輪の厚み（蹄から頭頂） | 60.4 mm |
| 子ヒツジ粗形の長さ | 99.2 mm |
| 一体分の角度 | 7.5°（1つの輪から48体） |
| 鋸の刃厚（切りしろ） | 1.6 mm |

## 3. 実行した検証

`npm test` — 34 assertions（`tests/geometry.test.ts` / `motion.test.ts` / `view.test.ts`）

| 検証項目（課題文の一覧に対応） | どのテスト／画像で示したか | 結果 |
| --- | --- | --- |
| 一片を元位置へ戻すと輪材の外形と溝が再構成できる | `geometry.test.ts` の点内外モンテカルロ40万点＋`shots/v-19..21` | 不一致は2本の切りしろ（各1.6mm）の内側だけ。体積比 0.34 % |
| 一片の断面と輪材の対応断面が一致する | `geometry.test.ts`「the wedge sawn face is exactly the ring notch face」 | 最大偏差 2 µm 未満（同一多角形から生成） |
| 切断面が塞がれ、裏が空洞の殻でない | 閉多様体チェック（境界辺0・非多様体辺0）＋体積が解析値と一致 | 合格 |
| 木片の厚みと木目が分離前後で変わらない | `view.test.ts`「keeps the same geometry, vertex for vertex」 | 頂点配列が完全一致。シェーダはオブジェクト空間座標だけを読む |
| 切断途中で止めても分離前の状態が保持される | `input.mjs`「stopping the finger stops the cut」 | 合格 |
| 完全分離前に抜けず、抜いた後も輪と交差しない | `motion.test.ts`（スライド61点×ヨー25点×全頂点） | 交差0件、最小クリアランス 4 mm 以上 |
| 連続操作中にモデルの差し替えがない | `view.test.ts` | ジオメトリオブジェクトも頂点も同一 |
| 完成時に見えるのが彩色済みでなく同じ木の粗形 | `shots/v-14`, `shots/f-p-7`, `shots/f-l-7` | 未彩色。切断面は同じ材の柾目 |
| 縦横どちらでも切り出し元と結果を一緒に確認できる | `shots/f-p-7-revealed.png` / `shots/f-l-7-revealed.png` | 縦=上に輪・下に木片、横=左に輪・右に木片 |
| 20回の再プレイで破綻しない | `scripts/soak.mjs` | 下記 |

### 実際の指入力での通し確認 (`scripts/input.mjs`)

状態フックを一切使わず、Pointer / mouse イベントだけで最初から最後まで操作した結果:

```
PASS  持ち手をドラッグすると切断が始まる
PASS  裂け目が刃より先へ行かない
PASS  切りかけでは木片がまだ繋がっている
PASS  指を止めると切断も止まる            (0.1373 -> 0.1373)
PASS  繋がったままの木片は引き出せない
PASS  pointercancel でジェスチャが安全に終わり、状態は保たれる
PASS  入力中に画面を回転しても切断が保たれる
PASS  向き変えの目標角が新しい画面向きに追従する   (83° -> 48°)
PASS  指だけで最後まで切り分けられる
PASS  切り分けると引き出しの段階へ進む
PASS  指だけで木片を引き出せる
PASS  指だけで断面がカメラ正面を向くまで回せる
PASS  向き変えがちょうど目標角で止まる   (yaw 83.0 / yawTarget 83.0)
all input checks passed        (13/13、例外・コンソールエラーなし)
```

（このテストは「削り始めの一瞬だけ送りが強い」件を踏まえ、カメラの寄りが
追いつくのを待ちながら短いストロークを重ねる。一気に弾くと最初の一撃で
切り終わることがあり、その場合は「まだ繋がっている」系の確認がすり抜ける。）

## 4. 実装の構成

```
src/core/profile.ts    唯一の形の定義（閉じた2D多角形＝子ヒツジの横姿）＋寸法
src/core/sector.ts     多角形を2つの境界面のあいだで回して立体にする
src/core/blank.ts      準備済み輪材／木片／切りかけの状態
src/core/layout.ts     作業台まわりの寸法
src/scene/blankView.ts three.js のオブジェクト。木片は親の変換だけで動く
src/scene/tool.ts      切り分け鋸（背金付きの手鋸）
src/scene/workshop.ts  作業台、治具、押さえ具、受け台、木箱、窓
src/materials/wood.ts  木材シェーダ。輪材座標だけを読む
src/game/state.ts      三段階の状態機械と禁止事項
src/game/camera.ts     画面向きごとの構図とフィッティング
src/main.ts            入力（Pointer Events）、音、カメラ、再プレイ
```

### 木目が泳がない理由

木材シェーダは **オブジェクト空間座標だけ** を読む。輪材も木片もジオメトリを
輪材座標のまま持っており、動かすのは親の変換だけなので、木片が動いても
材料座標は頂点と一緒に動く。年輪は旋盤軸と同心（＝丸太の輪切りから輪を挽けば
そうなる）で、木片の木目は輪材の木目の literal な続きになっている。
切断面は年輪を縦に切るので柾目の縞になり、これも同じ材から導かれる。

## 5. 三段階の操作と、そこで守っていること

| | 操作 | 守っていること |
| --- | --- | --- |
| 1 | 鋸の持ち手を一指でドラッグ → 刃が半径方向に送られる | 裂け目 = 刃の到達半径の最小値。指が止まれば止まる。引き戻しても接着しない |
| 2 | 分離した木片を広い当たり判定でつかんで引き出す | 完全分離するまで動かない。輪は押さえ具で固定 |
| 3 | 同じ木片を机の上で滑らせて向きを変える | 輪から完全に離れるまで回転しない。回したあとは輪へ戻せない |

第3段階の目標角は **カメラ方位から導出** している（`yawTarget = 90° − azimuth`）ので、
縦でも横でも「回しきると断面がカメラ正面を向く」。画面を回転すると目標角も
追従する（縦83°／横48°）。

## 6. 性能

`npx tsx scripts/perf-geometry.ts`（Node、1コア）:

```
初回の静的ビルド  24-60 ms（一度だけ）
  輪材バルク  51,884 三角形 / 26,439 頂点
  木片バルク   2,786 三角形 /  1,890 頂点   ← 主役なので周方向を0.75°で掃引
  低品質時    輪材 28,952 / 木片 1,316
切断中の毎フレーム再構築  0.35-0.45 ms  (1,909 頂点 / 1,794 三角形)
主役の合計三角形数（高品質） 56,464
```
（この環境は他の検証プロセスと CPU を取り合っているため、時間値には幅がある）

切断中に作り直すのは切断面まわりの「カラー」2枚だけで、バッファは最悪ケース分を
最初に確保して描画範囲を動かすので、**削っているあいだの追加確保はゼロ**
（`view.test.ts` で検証）。

ブラウザ実測（`scripts/perf-runtime.mjs`）: 描画コール 55–56、毎フレーム
約 36,000 三角形。

### 指の移動量（`scripts/perf-runtime.mjs` ではなく `scripts/stroke.mjs`）

切り分けきるのに必要な指の移動距離を、実際のポインタ操作で計測した:

```
縦 430x932 : 212 css px  (短辺の 49 %)
横 932x430 : 368 css px  (短辺の 86 %)
```

刃の送りは指の移動と1:1（世界座標での対応）なので、当初は縦画面で
133 px＝短辺の31%しかなく、指を弾いただけで切り終わってしまっていた。
削り始めるとカメラが作業側へ寄る構図に変えて、いまの値になっている。

## 7. 未検証・限界・判断したこと

1. **一次資料に到達できなかった。** このセッションのネットワークは
   egress proxy によって許可ドメインのみに制限されており、
   `spielzeugmuseum-seiffen.de` と `reifendrehwerk.com` はどちらも
   CONNECT が 403 で拒否された（`curl` でも WebFetch でも同じ）。
   したがって「STAMM - RING - TIER」の展示内容そのものは確認できていない。
   実装は課題文に書かれた構造（輪材の断面に動物の輪郭が含まれ、切り分けた
   粗形へ後から彫刻と彩色を行う）に忠実に従っている。
   **画像素材の転載は一切していない**（木材・工具・工房はすべて手続き的生成と
   基本形状）。寸法は本作の設計値であって、資料の実測値ではない。
2. **子どもが実際に「ひつじ」と認識したかは未検証。** 試遊していない。
   本レポートで言えるのは形状と操作の検証結果までである。
3. **実機（iPhone / iPad）のフレームレートは未計測。** この環境には GPU がなく、
   Chromium は SwiftShader（ソフトウェア描画）で動いている。参考値として
   ソフトウェア描画での中央値は 394 ms/フレーム（縦・約3fps）だが、これは
   実機性能の指標にならない。実機で測るまで「60fps 達成」とは言えない。
   分かっているのは、主役が約 55,000 三角形、描画コール 55、切断中の
   CPU 追加コストが 1 コアで 0.34 ms/フレーム、切断中の確保がゼロ、という点まで。
   低品質へ自動的に落とす経路（ピクセル比・影解像度・輪材の周方向分割）は
   実装してあるが、実機で発火したことは確認できていない。
4. **タイトル画面に「ひつじ」と書いてある。** 課題文が子ども文として
   その一文を指定しているため、そのまま出している。ただし
   「最初の謎は何の形が入っているか」という要求とは緊張関係にあり、
   ネタバレになる。プレイ中は動物名・答え合わせアイコン・鳴き声を一切使わず、
   認識は輪郭だけで成立する（音を消しても成立する）。
   一文を出さない、あるいは終了後に出す運用も `index.html` の1箇所で切り替えられる。
5. **横画面では断面が縦画面ほど「真横から隠れて」いない。** 横画面は
   「輪材と木片を左右に比較できる構図」を優先して方位42°を採っており、
   引き出し中の断面の見え方は縦画面（方位7°、ほぼ真横＝ほとんど見えない）より
   早い段階で読める。回しきったときに正対するのは両方向とも同じ。
6. **鋸は「台に支持されたガイド」ではなく手鋸にした。** 一度は台上にレールを
   渡す実装にしたが、レールをどの高さに置いても、最後に子どもが粗形を読む
   まさにその瞬間に画面を横切ってしまった。いまは入力側で送り軸に拘束し
   （細い線をなぞらせない）、切り終わると引き抜いて台へ置く。

## 8. 確認画像 (`shots/`)

無地環境（工房なし。`?plain=1&orbit=1`）:

| ファイル | 内容 |
| --- | --- |
| `v-01..04` | 準備済み輪材を斜俯瞰・真上・真横・背面から。動物には見えない |
| `v-05` | 準備済みの切りしろ（θ0）の接写。1.6 mm の鋸目 |
| `v-06..08` | 切断 25% / 60% / 98%。裂け目は常に刃の到達点まで |
| `v-09` | 鋸を引き戻しても裂け目はそのまま |
| `v-10..12` | 引き出し 30% / 70% / 全部。輪と木片のあいだの隙間 |
| `v-13` | 回しきって断面が正対 |
| `v-14..18` | 取り出した粗形を正面・小口・背面・真上・低い斜めから |
| `v-19..21` | **元位置へ戻した状態**。外形と溝が連続し、残るのは2本の鋸目だけ |

工房・実プレイのカメラ（`f-p-*` 縦 430×932 / `f-l-*` 横 932×430）:

| ファイル | 内容 |
| --- | --- |
| `1-start` | 溝の多い輪材と工具。断面の横姿は見せていない |
| `2-cut-half` | 工具・切断位置・一片の境界が同時に見える |
| `3-parted` | 切り分け完了。鋸は引き抜かれて台へ置かれる |
| `4-pull-gap` | 輪と一片のあいだに開く隙間 |
| `5-out` | 木片が受け台の上。まだ小口向きで動物には読めない |
| `6-turning` | 向きを変えている途中 |
| `7-revealed` | 同じ木片が子ヒツジとして読める。背後に自分が抜けた欠損 |

`i-finger-driven-result.png` は状態フックを使わず指入力だけで到達した最終状態。
`r-*.png` は再プレイの各コマ。`r-0-*` で欠損した輪材と粗形が運び去られ、
`r-1-*` で無傷の準備済み輪材が治具へ入る。切断の逆再生は起きていないし、
粗形が輪へ戻る様子も映らない（画面外で入れ替えている）。鋸は台に残る。

## 9. この試作でしていないこと（課題文の範囲外として）

旋盤で輪材を作る工程、細部の彫刻、彩色、動物図鑑、収集ゲーム、動物の種類追加。
毎フレームの立体ブーリアン演算（不要だった。事前に整合した掃引で足りる）。

## 10. 途中で見つけて直した不具合（記録）

| 症状 | 原因 | 対処 |
| --- | --- | --- |
| 木片と輪材の体積が解析値と合わない | 切断面の三角形の巻き方向が裏返っていた。切断面が軸を通る場合は発散定理の寄与が0になるため、平面が軸を含む間はテストをすり抜けていた | 巻き方向を修正。体積が解析値と6桁一致するテストを追加 |
| 輪の周りに放射状の縞が出る | `annularSector` が全面裏返しで、治具と受け台が裏面を影マップへ書いていた | 巻き方向を修正。巻き方向と法線の一致・体積の符号を見るテストを追加 |
| 輪材の一箇所に縦の継ぎ目 | 木材シェーダが `atan()` を使っており ±π で不連続だった | 角度ではなく単位方向ベクトルを入力にした |
| 削るあいだ毎フレーム型付き配列を確保 | 切断面のカラーの頂点数が切り込みの進行で変わる | 最悪ケース分を一度だけ確保して描画範囲を動かす。確保ゼロをテストで固定 |
| 木片を真ん中でつかむと回転が暴れる | 回転軸まわりの接線方向が、軸上では未定義 | 鼻先を固定のてこ腕として使う写像に変更 |
| 完成した粗形の上に工具が横たわる | 台上のレール（ガントリ）がカメラと粗形のあいだに入る | レールを廃し、手鋸＋入力側の拘束に変更。切り終わると引き抜いて台へ置く |

## 11. 実装上の判断（課題文の解釈）

- **「支持された工具を一方向へ送る」** — 送り軸への拘束を入力側（ポインタの
  射影）で行い、細い切断線をなぞらせない。可視のガントリは置かない（理由は §7-6）。
- **初期カメラ** — 縦は方位7°（切断面をほぼ真横から見るので断面は見えない）、
  横は方位42°（輪材と木片を左右に比較する構図を優先）。回しきったときに
  正対する角度は方位から導出しているので、どちらでも成立する。
- **鋸の刃厚を切りしろとして実在させた** — 刃と裂け目が同じ 1.6 mm。
  これが「戻したときの許容誤差」の正体でもある。
- **切りかけの表現** — 立体ブーリアンではなく、境界面を「ある半径より外側だけ
  横にずらす」ことで表現している。毎フレーム作り直すのは切断面まわりの
  薄いカラー2枚だけ。

## 12. 20回の再プレイ (`scripts/soak.mjs`)

各回とも「削る → 抜く → 回す → 完成 → 再プレイ」を通し、毎回の
WebGL リソースと JS ヒープを記録した。

```
round 0  phase done  geometries 56  textures 3  programs 5  collarAllocs 2  heap 9.4 MB
round 5  phase done  geometries 56  textures 3  programs 5  collarAllocs 2  heap 7.7 MB
round 10 phase done  geometries 56  textures 3  programs 5  collarAllocs 2  heap 8.5 MB
round 15 phase done  geometries 56  textures 3  programs 5  collarAllocs 2  heap 8.9 MB
round 19 phase done  geometries 56  textures 3  programs 5  collarAllocs 2  heap 9.2 MB

rounds completed: 20
geometry delta: 0      texture delta: 0      program delta: 0
heap delta MB: -1.4    page errors: []
final state: { plays: 20, cut: Infinity, slide: 0, yaw: 0, phase: 'cut' }
```

20回すべてが `done` に到達し、ジオメトリ・テクスチャ・シェーダプログラムの数は
一切増えず、カラーバッファの再確保も最初の2回（木片側と輪材側の1回ずつ）だけ。
ヒープは 7.3〜9.4 MB を上下するだけで増加傾向はない。例外・コンソールエラーなし。
最後の状態は「未切断の準備済み輪材」に戻っている。


## 13. 分かったが直していないこと

- **削り始めの一瞬だけ、送りの利きが強い。** 刃の送りは指の移動と世界座標で1:1に
  対応させており、削り始めるとカメラが作業側へ寄る。寄りきる前（最初の 0.2 秒ほど）
  は広い構図の縮尺のままなので、その間だけ同じ指の移動量で深く切れる。
  規則（指の移動に対応する／止めれば止まる／逆入力で接着しない）は破っていないが、
  一気に指を弾くと最初の一撃で切り終わってしまうことがある。
  `scripts/input.mjs` はこれを踏まえてカメラの追従を待ってから測っている。
- **横画面の切り分けは指の移動量が長め**（短辺の86%）。縦（49%）に比べて
  一回のストロークで届きにくく、持ち替えが要るかもしれない。
