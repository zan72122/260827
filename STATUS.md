# STATUS — はじめて音になる、オルゴール工房

作品: シリンダー式オルゴールの噛み合い調整を、4 歳児が指一本でやる Web ゲーム。
対象: iPhone / iPad の Safari を想定 (未検証。下の「未確認」を参照)。

起動先: `npm install && npm run dev` → http://127.0.0.1:5173/
ビルド確認: `npm run verify` (型検査 → 単体テスト → 本番ビルド)
画面証拠: `npm run evidence` → `docs/evidence/`

---

## 1. 成立した操作

| 段階 | 何が起きるか | 実装の根拠 |
| --- | --- | --- |
| 起動 | 未調整のメカが机の上にある。保持ねじは緩んだまま。ピンは歯先の手前 0.10 mm を空振り。 | `MESH.initialClearance = 0.10` |
| 調整 | 手前のローレットドラムを転がすと、櫛歯ブロックが半径方向 (`U_RADIAL`) にだけ動く。 | `Workshop.setTravel` / `Mechanism.setEngagement` |
| 試し回し | ハンドル → 丸ベルト → シリンダー。ピンが歯を押し、離れた瞬間に鳴る。 | `stepPasses` の release 交差 → `AudioEngine.release` |
| 固定 | ねじ回しの太い把手を短い円弧で回すと、ねじ頭が実際に回って沈む。 | `Mechanism.setScrew` |
| 演奏 | 同じメカを回すと、その発音が続いて短い旋律になる。 | ピン 21 本 = 音符 21 個 |
| 再調整 | 「ねじを ゆるめる」で調整へ戻る。ケース製作はやり直さない。 | `Workshop.loosen` |

一指の Pointer Events。二本目の指は無視、`pointercancel` / 範囲外移動 / 持ち替えを処理。
画面回転で噛み合い量 (`travel`) も曲の位相 (`theta`) も失われない (状態は画面サイズに依存しない
モジュールに置き、さらに `sessionStorage` へ保険で保存)。時間制限なし。

---

## 2. 確認した結果

### 単体テスト (`npm run test`) — 38 件すべて成功

### E2E (`npm run evidence`) — 12 件すべて成功

### ビルド (`npm run build`) — 型検査 (`tsc --noEmit`, strict) を通ってから成功

検収項目との対応:

- **隙間が大きい設定では、ピンが通過しても楽音が出ない** —
  起動時設定で 3 回転させても発音イベント 0 件。600 分割の細かい刻みでも 0 件。
- **適正設定では、接触 → たわみ → 解放と発音が一致する** —
  ピン 1 本・歯 1 枚で contact / release がこの順に 1 回ずつ。イベント角度が
  ピン中心 ∓ 接触半幅と一致。接触区間の内側だけ歯がたわむ。
- **ハンドルを止めると新規発音が止まり、同じ角度に留まっても連打音にならない** —
  角度が進まないステップは 240 回連続でイベント 0 件。接触の途中で止めると
  歯はたわんだまま保持され、発音しない。
- **噛み合い量を変えても、歯に割り当てた音程が別の音へ変わらない** —
  travel 0.11〜0.72 のすべてで、同じピンは常に同じ歯だけを弾く。変わるのは強さだけ。
- **低いフレームレート相当でも、通過イベントが重複せず状態が安定する** —
  120 / 60 / 30 / 15 / 8 / 4 fps 相当で 2 回転させ、21 本すべてがちょうど 2 回ずつ。
  10 回転を 1 ステップで渡しても重複なし。刻み方を変えても発音列が同一。

### 実画面・実ポインタ操作 (`npm run evidence`, Playwright + Chromium)

- 空振り: 起動状態でハンドルを 1.2 回転ぶん回しても releases = 0, contacts = 0,
  全歯のたわみ 0。→ `docs/evidence/01-miss-390x844.png`
- 接触中 / 解放後: ピンの真上で停止したとき最大たわみ > 0.25 mm、
  通過後は < 0.02 mm。→ `02-contact-390x844.png` / `03-released-390x844.png`
- 止めたら止まる: 指を離して 1.5 秒待っても releases も theta も増えない。
- 逆回し: 逆方向に半回転ぶん動かしても theta は 1 e-6 以内で不変。
- 音程不変: travel 0.2 / 0.45 / 0.7 で、24 角度すべてにおいて
  「たわむ歯の番号」の並びが完全一致。
- 低フレームレート: CDP で CPU を 12 倍遅くした状態で 2 回転させ、
  releases = 42 / contacts = 42 (= 21 本 × 2)。→ `04-lowfps-390x844.png`
- 390×844 / 844×390 / 820×1180 / 1180×820 の 4 画面すべてで、
  調整 → 試し回し → 固定 → 演奏 を一本の指で完了。
  → `10-adjust-*.png` `11-crank-*.png` `12-tool-*.png` `13-play-*.png`
- 同じ幾何から空振り / 接触中 / 解放後 の接写を作れる (確認用の別カメラ。
  機構そのものは一切変えていない)。
  → `30-closeup-miss.png` / `31-closeup-contact.png` / `32-closeup-released.png`
- 完成後は作業構図のまま数音鳴らしてから引く。→ `20-play-worksetup.png` / `21-reveal-workshop.png`
- 上記のテスト中、ページエラーとコンソールエラーは 0 件。

### 描画の負荷 (`tools/perf.mjs`)

| 画面 | draw call | 可視三角形 | DPR |
| --- | --- | --- | --- |
| 390×844 | 66 | 12,992 | 1.0 (上限 1.5) |
| 844×390 | 71 | 13,904 | 1.0 (上限 1.5) |
| 820×1180 | 70 | 13,708 | 1.0 (上限 1.5) |
| 1180×820 | 69 | 13,696 | 1.0 (上限 1.5) |

目安 (draw call 90 以内 / 可視三角形 15 万以内 / 主要な動的影 1 系統 /
通常 DPR 上限 1.5) の範囲に収まっています。動的な影は作業灯の 1 系統だけ、
ピンと櫛歯と錘は instancing、材質は共有です。判定は毎フレーム
「ピンの本数ぶん」しか回りません (全ピン × 全歯の総当たりはしません)。

**フレームレートはこの環境では測れていません。** 手元の実行環境の WebGL は
SwiftShader (CPU ラスタライザ) で、実測すると 160×160 で 12 fps、320×320 で
5 fps、390×844 で 2 fps と、ほぼ画素数に反比例します (約 30 万画素/秒)。
つまり完全にフィルレート律速で、これは実機 GPU の性能とは桁が違います
(モバイル GPU は毎秒 10 億画素の桁)。したがって
**「30 fps を安定した下限、余力があれば 60 fps」は実機で未検証** です。
構造側の目安 (draw call・三角形・影・DPR) だけが確認できた項目です。

---

## 3. 作中治具と、その簡略化

**これは現物工場の治具ではありません。** 実物の噛み合い調整は数十 µm の作業で、
4 歳児が指一本でできる大きさへ翻訳するために、この作品用の治具を設計しました。
寸法・許容域・調整倍率はすべて `src/core/spec.ts` に記録してあります。

| 項目 | 作中の設計値 | 備考 |
| --- | --- | --- |
| メカの幅 | 82 mm (ベッドプレート) | 設計の出発点。実測値ではない。 |
| シリンダー | 半径 6.5 mm / ピン列長 34 mm | |
| ピン | 半径 0.22 mm / 突出 0.9 mm | 先端円の半径 7.4 mm |
| 櫛歯 | 12 枚 / ピッチ 2.35 / 板厚 0.55 / 自由長 13.2 mm | 歯先は一直線に揃う |
| 起動時の隙間 | 0.10 mm | ここが空振りの正体 |
| ストッパー | 実変位 0.72 mm まで (噛み合い深さ 0.62 mm) | 歯先を壊す深さまで押し込めない |
| 楽音になる下限 | 噛み合い 0.028 mm | これ未満は擦れる機械音だけ |
| 固定してよい下限 | 噛み合い 0.075 mm | |
| 調整倍率 | ドラムの縁 43.6 mm の転がりで櫛歯 1 mm | 240° の回転で 0.72 mm (送りねじ 1.08 mm/回転) |
| ハンドル比 | ハンドル 1 回転でシリンダー 1.30 回転 | 丸ベルト φ8.5→φ6.5 相当 (`pulleyHandle/pulleyCylinder`) |
| ガバナー | シリンダー最大 2.83 rad/s (約 0.45 回転/秒) | 実機の空気制動に相当する速度制限 |
| 工具 | 把手の中心がねじ頭から 5.6 mm | 作業構図で画面上 80〜110 CSS px。指がねじ頭を隠さない |
| 固定ねじ | 2 本、歯の根元より奥 (x = ±8.5 mm) | どの画面でも指が届く間隔 |

### 意図的に簡略化した点

1. **歯の調律**: 実物は歯の幅と先端の鉛錘で音程を決めます。本作も幅と錘を持たせて
   いますが、質量と固有振動数の関係を式で解いてはいません。音程は固定表
   (`TOOTH_HZ`) で、幅と錘は見た目の根拠として √f に沿って割り当てています。
2. **歯のたわみ形状**: オイラー梁の静たわみ `y(x) = δ(3(x/L)² − (x/L)³)/2` を
   頂点シェーダで適用しています。動的モードは無視しています。
3. **たわみの向き**: 歯は自分の面に垂直にたわむので、半径方向に e だけ逃げるには
   e / cos(20°) だけ曲がります。この係数 (`BEND_PER_RADIAL`) を掛けて、
   歯先が半径方向にちょうど e だけ逃げるようにしています。
4. **接触位置**: ピンは歯先から約 2.3 e mm 以内の範囲に当たります (櫛の傾き 20°)。
   判定では歯先 1 点として扱っています。
5. **発音後の残響振動**: 実際は数百 Hz です。巨大な歯がゆっくり揺れる運動には
   置き換えず、実周波数のまま振幅 0.05 mm 以下・120 ms 減衰の微細なブレとして
   描いています (`EXAGGERATIONS.releaseShimmerAmplitude`)。誇張はここだけです。
6. **発音の強さ**: 物理的には変位の二乗に比例しますが、「はじめて鳴った一音」が
   聞こえないと因果が伝わらないため、0.3〜1.0 に圧縮しています
   (`releaseLoudness`)。深いほど強い、という向きは保っています。
7. **ベルト**: 丸ベルトは二つの車に本当に巻き付いた閉ループとして作ってありますが、
   ベルト自身が走る様子 (表面の移動) は描いていません。
8. **音響**: 片持ち鋼片らしい非整数倍音 (1 : 4.19 : 8.63 : 13.4) の加算合成と、
   その場で合成した短い響板 IR です。物理モデリングではありません。
9. **旋律**: この作品のための短い自作フレーズ (24 拍 / 1 回転 / ピン 21 本)。
   既存製品の録音にも採譜にも依存していません。

### 守った線

- 櫛歯を近づける操作は **音程を合わせる調律ではありません**。噛み合い量を変えても
  歯と音程の対応表は一切変わりません (単体テストで固定)。
- 接触していないピンからは音が出ません。発音は解放イベントからしか起きません。
- 曲を鳴らすための独立した BGM タイマーは存在しません。止めれば新しい音符は生まれず、
  既に鳴った音の余韻だけが減衰します。
- スコア、正解ランプ、長い説明、派手な完成演出はありません。子ども文は一行だけで、
  最初の一音が出たら消えます。

### 資料の扱い

- 参照したのは構造の事実だけです (ピンが櫛歯を弾いて発音すること、調律とは別に
  噛み合い調整という工程があること)。
- 資料画像の転載はしていません。ゲーム中に外部ホットリンクはありません。
  依存物はすべて同梱で、開始後はネットワークがなくても操作できます
  (テクスチャも環境マップも響板 IR も、その場で合成しています)。

---

## 4. 未確認事項

1. **実機 Safari は未確認です。** iPhone / iPad の Safari は手元にありません。
   画面証拠はすべて **Chromium (SwiftShader)** で取得したものです。
   **WebKit エミュレーションでもありません** — Playwright の webkit プロジェクトは
   使っていません。したがって次は未検証です:
   - iOS Safari の AudioContext の起動・復帰の挙動
   - iOS の Pointer Events / pointer capture の細部
   - `env(safe-area-inset-*)` のノッチ回り込み
   - 実機 GPU での実フレームレートと発熱
2. **実際の 4 歳児の反応は観察していません。** 「20〜30 秒で少なくとも一度、
   自分の調整後に見える接触から一音が出る」「一指で順番に扱える」「壊れる失敗が
   ない」は **設計仮説のまま** です。当たり半径 (ドラム 52〜150 px, ハンドル
   56〜170 px, 工具 74 px)、初期隙間 0.10 mm、必要なドラッグ量 (約 120 px で
   楽音域へ到達) はいずれも観察前の設定値です。
3. **音の聴感は未確認です。** 自動テストは発音イベントの数と対応関係だけを見ています。
   音量・音色・耳の痛さは実機の耳で確かめていません。
4. **完成後の「引き」の構図は、縦画面では工房の小物が入りきりません。**
   横画面では作業灯・部品トレー・もみの枝・赤い飾り玉が入りますが、縦画面は
   幅が足りず、響板と作業台と部品トレーまでです (赤い飾り玉が右端にわずかに入る)。
5. **長時間の連続操作** (数十分) でのメモリ・音声ノードの蓄積は測っていません。
   ボイス数は 26 で頭打ちにし、終了したノードは切り離していますが、実測はしていません。
6. **色覚特性や、明るい屋外での視認性** は検討していません。
7. **音声の二重スケジューリング**は、描画ループが止まれば発音イベントも
   生まれない構造で防いでいます (タブ復帰時に刻みをやり直し、音声時計は毎フレーム
   読み直す)。ただし実機での復帰・着信割り込みは試していません。
