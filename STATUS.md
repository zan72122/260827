# はじめてうなずく、クリスマスべこ工房 — STATUS

会津張り子の牛をもとにした、**首組み**だけの一作品です。和紙の成形・乾燥・下地・彩色は
済んだ部品から始まり、子どもがするのは重りの釣り合わせ、頭の差し込み、糸の長さ決め、
そして最初のひと押しだけです。

子ども文：**「頭をつけて、うしさんをゆらゆらさせる」**

---

## 起動

```bash
npm install
npm run dev            # http://localhost:5173 （--host 有効、LAN の実機からも開けます）
```

そのほか：

```bash
npm run typecheck      # tsc --noEmit
npm run test           # vitest（32 件）
npm run build          # 型検査 + 本番ビルド → dist/
npm run preview        # ビルド結果を :4173 で配信
npm run capture        # Playwright で 4 画面サイズを実操作し docs/evidence/ を更新
```

iPhone / iPad で開く場合は `npm run dev` が表示する `Network:` の URL を、
同じ Wi-Fi 上の Safari に入力してください。ビルド後の `dist/` は静的ファイルだけで、
外部への通信を一切必要としません（依存物・テクスチャ・音はすべて同梱・生成）。

---

## 何が起きるか

| 工程 | 子どもの操作 | 動いているもの |
| --- | --- | --- |
| `balance` | 仮治具に吊るした頭の、太い取っ手を動かす | おもりがレール上を滑り、頭の重心が動く。静止姿勢が連続して変わる |
| `insert` | 頭をつかんで運ぶ | 開口の軸に沿った実際の経路。首の後方部分から入る |
| `thread` | 糸端の木製つまみを引く | 糸のたるみが減り、支持点が上がる。頭が受け台から浮き、縁との隙間が開く |
| `tie` | 縁のピンを一度たたく | 結び目ができ、受け台と仮治具が外れる |
| `firstNod` | 頭を軽く押して離す | 胴は止まったまま、頭だけがうなずく |
| `play` | 何度でも触る | 触るたび。やり直しも祝福演出もなし |

首振りは**用意された動画やアニメーションではありません**。頭・首・内側の腕・おもりを
一つの剛体として、支持点まわりの重力復元トルクと減衰で積分しています。おもりを動かすと
重心の位置が変わり、それが同時に「静止姿勢」と「戻ろうとする強さ（＝首振りの速さ）」の
両方になります。`src/sim/rig.ts` に全部入っており、成功フラグで別の再生を始める箇所は
ありません。

---

## 合格条件と、確認したこと

数値は `docs/evidence/capture-log.json`（実際のポインタ操作で駆動した記録）から。
画面証拠は `docs/evidence/*.png`。

| 条件 | 結果 | 証拠 |
| --- | --- | --- |
| おもりを動かすと静止姿勢が変わり、見える位置と運動パラメータが食い違わない | 取っ手の操作で `weightT` 0.15→0.37、静止姿勢 16.8°→2.6°。固有角振動数も同じ重心から算出（周期 0.52–0.68 s） | `*-1-before-balance.png` / `*-2-balanced.png`、`tests/rig.test.ts` |
| 頭・糸・胴の支持位置がつながり、完成後に無支持で浮いていない | 支持点は常に糸のたるみから決まる。`always hangs from the thread, never in mid-air` | `*-7-finished.png`、`tests/rig.test.ts` |
| 糸を引くと頭が上がり、開口との隙間が生まれるのを一画面で追える | 縁との隙間 3.2 mm → 10.4 mm、受け台からの浮き 7.7 mm。カメラは同じショットのまま | `*-3-seated.png` → `*-4-lifted.png` |
| 軽く押して離すと頭だけがうなずき、胴と四脚は接地したまま | 胴のトランスフォームは不変。四脚は殻の外壁下に接合、床 y=0 に接地 | `*-6-first-nod.png`、`*-8-touch-again.png` |
| 小さい操作と大きい操作の応答が違い、どちらも減衰して止まり、内部を貫通しない | 軽い一触りと 96 px のドラッグで振幅が明確に異なる（下の表）。接触限界は殻の内壁から角度で解いてクランプ | `*-9-firm-push.png`、`tests/rig.test.ts` の貫通テスト |
| 断面表示と外観表示で同じ頭・支持点・重りを保つ | 断面は同じ胴の内外面を平面で切って生成し、切断面は壁厚をもつ帯。頭・腕・おもりは同一メッシュで、切り替えは平面の移動だけ | `*-4-lifted.png`、`tests/geometry.test.ts` |
| 390×844 / 844×390 / 820×1180 / 1180×820 で最後まで遊べる | 4 画面すべてで `play` に到達 | `capture-log.json` |
| 途中の画面回転や pointercancel でも復帰できる | balance 中に pointercancel → おもり位置 0.07 のまま保持、390×844→844×390 に回転 → 保持、thread 中に 820×1180 へもう一度回転しても最後まで到達（`play`） | `rotate-midflow.png`、`resilience-finished.png` |

### 首振りの大小

4 画面すべてで計測した、静止姿勢からの最大振れ（`capture-log.json` の `nod-peaks`）：

| | 390×844 | 844×390 | 820×1180 | 1180×820 |
| --- | --- | --- | --- | --- |
| 軽く一触り | 7.1° | 7.2° | 7.0° | 7.2° |
| 96 px の押し込み | 15.3° | 14.7° | 19.5° | 19.5° |

同じ剛体の同じ解放です。初速が違うだけで、どちらも同じ減衰で数秒かけて止まります。

---

## 参照したものと、断定しないこと

構造の参照元（別々の事業者の公開情報です）：

- 張り子と頭部取付の工程 — https://warabi-akabeko.com/about-akabeko/
- 首の仕掛けと調整 — https://akabekoland.buyshop.jp/blog/2026/04/28/090334
- クリスマス関連商品が実在すること — https://nozawa-mingei.com/shop/ （2025年10月24日の告知）

**参照したのは「木型から作る中空の和紙の張り子」「別体の頭部」「頭部のおもり」「紐による
支持」「首振りのバランス調整」という、公開情報から確認できる構造だけです。**

- 一社の全製法を確認したとは主張しません。上の三つは別々の事業者の資料です。
- 特定のクリスマス商品の内部を忠実に再現したとは主張しません。作中の人形と治具は、
  形・構造・材質を参照したオリジナルです。
- 資料の画像は転載していません。実行時に外部を読みにも行きません。テクスチャは
  すべて起動時に canvas で描いています（`src/render/materials.ts`）。

### ゲーム独自の簡略化（現場の工具ではありません）

1. **取っ手（GRIP）** — おもりに留めた木のつまみです。世界の中ではおもりと 1:1 で動き、
   「大きな操作 → 小さな移動」の縮約は**ポインタの写像側**にあります（レール全長 11 mm を
   190 CSS px のドラッグに割り当て）。実在の治具の再現ではありません。
2. **仮固定のおもり** — 釣り合わせの間だけ、おもりに紙の輪と取っ手が留めてあります。
   胴に入れる前に外れます。
3. **糸結びの補助動作** — 適正な長さを決めて手を離したあと、縁のピンを一度たたくと
   結ばれます。実際には結束と乾燥に時間がかかる工程を、短い一操作に縮約しています。
   完成後も糸・固定位置（2 本のピン）・結び目は実在の立体として残ります。
4. **受け台（あご受け）** — 糸が張るまで頭のあごを支える木の台です。工程を読めるように
   置いた作中の道具です。
5. **クリスマスらしさ** — 胴に描かれた白い雪の点、緑と白の紐模様、柊です。**塗りだけ**で、
   質量も干渉も増やしません（釣り合わせたあとに重い飾りを足さない、という要件のため
   帽子は付けていません）。
6. **おもりの密度** — 直径 12 mm で 30 g としています。鉛程度としても密度は過大で、
   遊びの手ざわりを優先した値です。

---

## 寸法（作中の値。実測ではありません）

出発点は全長 160 mm 前後・全高 100 mm 前後・殻の厚み 0.8–1.5 mm。
すべて `src/sim/dims.ts` に一度だけ書かれ、メッシュと物理の両方がそこを読みます
（見えているものと動いているものがずれないため）。

| | 値 | 備考 |
| --- | --- | --- |
| 全長（尾端〜鼻先） | 約 158 mm | |
| 全高（床〜角の先） | 約 97 mm | 糸を引き切った状態 |
| 胴の殻の厚み | 1.0–1.5 mm | 首の開口に向けて厚く |
| 頭の殻の厚み | 1.0–1.2 mm | |
| 首の開口（外径） | 24 × 25 mm | 内径は壁厚ぶん小さい |
| 首の軸（stem）半径 | 6.2 mm、糸の溝で 3.5 mm | |
| おもり | 半径 6 mm・30 g、レール長 11 mm | |
| 糸 | 半径 0.68 mm | **読みやすさのため実物より太い**（実際の麻糸はもっと細い） |
| 脚の付け根 | y = 24 mm | 外壁の下に接合。初稿の 34 mm は胴の内側に突き抜けていたので下げました |

初稿から変えた主なもの：脚の付け根（34→24 mm、断面で腔内に脚が刺さっていたため）、
糸のピン位置（開口の縁から外側へ、z=±15 mm。首の軸を糸が貫通していたため）、
頭の重心高さ（支持点の上 27→12 mm。倒立振子として不安定だったため）。

---

## 性能

計測は Chromium（SwiftShader・ソフトウェアラスタライズ）上、DPR 2 の 4 画面サイズです。

- 可視三角形：**25,100 – 26,600**（目安 140,000 以内）
- draw call：**17 – 22**（目安 80 以内）
- 動的影：**1 系統**（窓光の平行光。作業灯は影を落としません）
- DPR 上限 1.5。フレーム時間が 1/34 s を超えたら DPR を 1.0 に、
  さらに 1/28 s を超えたら影を切ります。**背景と影から先に落とし、頭の輪郭・支持・
  入力応答は落としません**（`Game.adapt`）。
- 物理は 1/240 s 固定ステップ。1 フレームで進める上限は 1/12 s で、
  タブが隠れて戻ったときは経過時間を破棄します（`StepClock`）。

---

## 確認していないこと

- **実機の iOS Safari では未確認です。** 上の記録はすべて Linux 上の Chromium
  （WebKit ではありません）でのものです。WebKit エミュレーションも行っていません。
  実機がないため未確認と記します。iOS 特有の挙動（`AudioContext` の解錠、
  `visibilitychange` の間隔、safe-area、ピンチ抑止）はコード上は扱っていますが、
  実機での確認は済んでいません。
- **実際の 4 歳児では試していません。** したがって理解度、最初の 20〜30 秒で
  おもりの効果に気づけるか、もう一度触りたくなるかは、いずれも**未検証**です。
- 音は Chromium のヘッドレス環境では鳴らしていません。合成の実装のみ確認しています。
- 再プレイ（🐮 ボタン）はジオメトリもテクスチャも作り直さず、釣り合いの状態だけを
  入れ替えます（`Game.resetDoll`）。GPU 資源が増えないことはコード上そうしてあるだけで、
  長時間の連続再プレイでの実測はしていません。

## 既知の制約

- 頭の殻は閉じた中空形状で、首の軸はその壁を貫いて差し込まれています（実物なら穴が
  開いています）。断面は胴だけに用意しているため、この差は画面には出ません。
- 断面の窓は近い側の脇腹を平面で切って作ります。四脚は中実の形なので切らずに残します。
- 首振りは「うなずき」と小さな横揺れの 2 自由度です。糸そのもののたわみは形として
  描きますが、糸の力学は解いていません（要件どおり軽量モデル）。
