# 紙がひらく、クリスマス工房 — STATUS

平たいハニカム紙の束を指で開いて立体のクリスマスツリーにし、端をクリップで留める
モバイルWebの一作品。題材はハニカム紙のクリスマスツリーのみ。

---

## 1. 起動方法

```bash
npm install
npm run dev        # http://localhost:5173  （--host 0.0.0.0 で LAN 公開）
```

本番相当:

```bash
npm run build      # 型検査 + バンドル（dist/）
npm run preview    # http://localhost:4173
```

検査:

```bash
npm run typecheck  # tsc --noEmit
npm test           # vitest（構造の不変条件と状態遷移）
npm run test:e2e   # Playwright（4つの CSS viewport で最後まで操作）
npm run verify     # typecheck + test + build
```

依存はすべて npm 同梱（three / vite / typescript / vitest / @playwright/test）。
テクスチャ・環境マップ・音はすべて実行時に生成する。**ロード後に通信を必要としない。**
外部画像サーバーへのアクセスも、資料画像の同梱・ホットリンクも行っていない。

開発用パラメータ（通常プレイでは使わない）:

| クエリ | 意味 |
| --- | --- |
| `?dev=1` | 自由カメラ（OrbitControls）。`__dev.view('front'\|'side'\|'back'\|'top')` |
| `?open=0.5` | 展開量を直接置いて起動（画面証拠の撮影用） |
| `?fixq=1` | 性能に応じた品質の自動調整を止める |
| `?lite=1` | 検査用の軽量描画（影・背景・描画解像度を落とす。ジオメトリ・当たり判定・状態遷移・カメラは通常と同一） |
| `?seed=123` | 工房の小物配置の seed |

---

## 2. 参照した現実と、作中の創作範囲

### 参照資料

- 構造資料: <https://www.excelpoint.co.jp/denguri-honeycomb.html>
- 製造元: <https://www.excelpoint.co.jp/>

**この2つのページは、本作業環境のネットワーク送出ポリシー（egress proxy）により取得できなかった。**
したがって本作は、課題文に明記された核心
「紙の層ごとに糊の線を互い違いに配置し、その接着構造によって平たい束が開いて立体になる。
開くときの形は紙の枚数や糊幅にも影響される」
だけを出発点とした**参照ベースのオリジナル**である。
特定製品の完全再現でも、日本起源の技法の再現でも、実工場の再現でもない。
資料由来と称せる寸法・材質・工程は無い。以下の数値はすべて**作中の設計値**である。

### 作中の設計値（`src/design.ts`）

課題文で与えられた初期設計値:

| 項目 | 値 | 出所 |
| --- | --- | --- |
| 完成高 | 280 mm | 課題文 |
| 最大径 | 180 mm | 課題文 |
| 紙厚 | 0.04 mm | 課題文 |
| 端台紙厚 | 0.8 mm | 課題文 |

構造の整合のためにこちらで決めた値と、その理由:

| 項目 | 値 | 決めた理由 |
| --- | --- | --- |
| 紙の枚数 N | 24 | 全開時の隣接シート角 δ=π/N。セルの実寸が幅 ≈24 mm・高さ ≈30 mm になり、4歳児が「紙の間が開く」ことを目で追える大きさになる。増やすとセルが細くなりすぎ、減らすと紙が伸びる条件を割る。 |
| 糊の縦ピッチ P | 40 mm | 280 / 40 = 7 段ちょうど。段の切れ目が上下端に一致する。 |
| 糊帯の幅 w | 4 mm | 自由スパン hf = P/2 − w = 16 mm。拘束 R·δ = 90·π/24 = 11.8 mm < 16 mm を満たす（＝紙が伸びない）。 |
| 芯の内半径 | 0.306 mm | = 紙厚 / δmax。ここで隣接する紙の間隔がちょうど紙厚 1 枚ぶんになる半径。これより内側は穴にする。 |
| 完成時の頂点高さ | 280 mm（＝平たい紙の高さ） | 後述の縮みモデルでは軸上（u=0）が縮まないため、頂点の高さは平たい紙の高さと一致する。裾の広い部分は最大 12 mm ほど沈む。 |

---

## 3. ハニカムの実ジオメトリ（何をどう作ったか）

### 構造

- 紙は**水平な糊帯**で貼られる。界面 i（紙 i と i+1 の間）と界面 i−1 は縦に P/2 だけ
  ずれている ＝ 「層ごとに互い違い」。
- 束は**垂直軸まわりの扇**として開く。各紙は軸を含む平面（u = −R…+R）なので、
  扇が 0→(N−1)δ 回ると、半平面は 360° をちょうど一周ぶん覆う。
  両端台紙は最後に背中合わせで出会う。
- セルの筒は**半径方向**を向き、外から見ると六角の穴が並ぶ。

### 開閉の主状態は「展開量 t」ひとつ

`t ∈ [0,1]` から、次がすべて**同時に整合して**決まる（`src/honeycomb/panel.ts`）。

```
δ    = t · π/N                              隣接シート間の折れ角
ψ(y) = psiNorm(y) · δ/2                     各紙の周方向の折れ（糊帯で ±δ/2、間は直線ランプ）
s(u) = sqrt(1 − (|u|·δ / hf)²)              自由スパンの縦の縮み
Y(u,y) = glueBelow(y) + s(u)·freeBelow(y)   その高さの実際の高さ
p(u,y) = ( u·cos ψ, Y, −u·sin ψ )
```

`s(u)` の導出: 自由スパンの紙は長さ `hf` のまま上下の糊帯を結ぶ。開くと上下の糊帯は
周方向に `u·δ` ずれるので、残る縦の高さは `sqrt(hf² − (u·δ)²)`。
**紙は決して伸びない（s ≤ 1）。** `u = 0`（軸上）では `s = 1` なので頂点は動かず、
外周ほど沈む。**平らな輪郭が立体になるのはこの半径依存の縮みそのものである。**

- 貼られた高さでは隣接する紙の ψ と Y が完全に一致する（`glueBelow/freeBelow` は
  偶数枚・奇数枚・端台紙で共通）。よって**途中形状でも接着点は必ずつながる**。
- 閉状態と開状態で物体を差し替えていない。同じ頂点バッファを t で書き換えるだけ。
- 正六角形の穴を円錐に貼る方式でも、放射状の無接続な板でもない。ゴムのような伸びも無い。

### 閉じ切り付近の扱い

- 平たいときは剛体の積層オフセット（紙厚 0.04 mm ずつ）で層を分ける。
- 開くにつれてそのオフセットは 0 に寄り、代わりに扇の角度が層を分ける。
  芯半径 `uMin = τ/δmax` で両者がちょうど入れ替わるように選んである。
- 糊帯では 2 枚が同じ角度になるので、`±τ/2 · t` の法線方向のずれを頂点側に持たせ、
  **どの t でも貼られた 2 枚はちょうど紙 1 枚ぶんだけ離れている**（自動検査あり）。
  これにより閉じ切り付近の面重なり・z-fighting・セルの潰れが起きない。
- 法線は毎回格子差分から作り直し、両面材質 + `gl_FrontFacing` で裏返りを扱う。
- LOD は使っていない。**セル数は操作中に一度も変わらない。**

### 端台紙

- 0.8 mm の中実（表裏 2 面 + 全周のリム）。紙とは別の材質・色。
- 紙と同じ `Y(u,y)` に従わせている。**これは作中の簡略化**で、実物の 0.8 mm の板紙は
  ここまでたわまない。剛体の平面にすると外周側で最大 12 mm の食い違いが出るため、
  接着の整合を優先して「わずかにたわむ板紙」として扱った。
- 平たいときは束の厚みぶん外側、全開では 2 枚がちょうど背中合わせで触れる位置に
  なるよう、法線方向のオフセットを `τb/2 + τ/2 + ((N−1)/2)·τ·(1−t)` とした。

### 紙の厚み

- 紙 1 枚の切断端は 0.04 mm。実体として外周と底に幅 0.04 mm のリム帯を持たせてある
  （＝正面写真を貼った板ではない）。画面上は 0.1 px 未満なので、見た目の「束の厚み」は
  24 枚が扇状に並ぶことと 0.8 mm の端台紙が担う。

---

## 4. 触り方とカメラ

- 一指だけで開閉・留め外しができる。小さな糊線や紙の端をつまむ精密操作は無い。
- **つかみ代**（未晒クラフト色の別素材の板）は端台紙から張り出しており、紙本体と
  見分けられる。当たり領域は画面座標で半径 34 CSS px（直径 68 px）。
- それとは別に、**手前の広いドラッグ帯**（画面下端から `max(120px, 高さの26%)`）が
  端台紙の代理になる。指で開くセルを隠さない位置に置いてある。
- **画面右へ引くと開く**。方向は常に同じで、短いストロークを何回でも継ぎ足せる。
  全開までは 390px 幅で約 2 ストローク。
- 指を止めれば止まる。逆に引けば戻る。離したときの弾性戻りは全体の 3% を上限とする。
  入力を無視して全開する演出は無い。
- 一周開くために指を裏側へ回す必要は無い。つかみ代は手前を通って左から右へ動く。
- カメラは斜め俯瞰の固定作業カメラ。開く瞬間・全開の瞬間にカットは入らない。
  クリップを留めたあとにだけ、連続した動きで少し引く。
- 縦画面は高さと手前の操作帯、横画面は開いた厚みと束の側面で組み直す（同じ絵の縮小ではない）。
- 通常プレイに自由カメラは無い。`?dev=1` で側面・背面・上面を確認できる。

---

## 5. 音・入力・描画

- 紙の擦れる音は開閉速度に比例。指を止めれば（触れたままでも）新しい擦過音は止まる。
- クリップは接触した瞬間だけ小さく鳴る。BGM も成功ファンファーレも無い。
- 音は**最初の明示的な操作**でだけ開始する。右上に消音ボタン。無音でも最後まで遊べる。
- Pointer Events + pointer capture。`pointercancel`、画面外への移動、途中の持ち替え
  （指を替えても位置が飛ばない）、二本目の指（掴み替え用に覚えるだけ）を扱う。
- `touch-action: none` は canvas だけに掛けている。
- 画面回転で進捗は失われない（状態はレンダラから独立）。
- WebGL2 必須。取得できない場合と初期化に失敗した場合は、永久ローディングではなく
  「もういちど」ボタンのある再試行画面を出す。
- 明示的な状態管理（`src/state/store.ts`）、seed 固定の乱数、差し替え可能な時計。
  展開量が変わらないフレームでは頂点を書き換えない（`Panel.update` が false を返す）。

---

## 6. 検証結果

すべて本コンテナ内での実行結果。GPU が無いためブラウザは **SwiftShader（ソフトウェアGL）**
で動いている。したがって以下は「動作と状態遷移の確認」であり、**性能の確認ではない**。

### 型検査・ビルド

```
$ npm run typecheck        → エラー 0
$ npm run build            → dist/ 生成（index 約 550 kB / gzip 145 kB, three を含む）
```

### 単体テスト（vitest, 25 件すべて成功）

`tests/unit/structure.test.ts`（15 件）は、展開量 t = 0 / 0.08 / 0.25 / 0.5 / 0.75 / 0.93 / 1 の
それぞれで、出力された頂点そのものから次を確かめる。

| 確かめたこと | どう確かめたか |
| --- | --- |
| 紙が伸びない条件を満たす | R·δmax = 11.8 mm < hf = 16 mm |
| 縮み率は常に 1 以下、軸上で 1、外周で 0.8 未満 | `shrinkAt` の単調性 |
| 互い違いの糊 | 偶数枚と奇数枚で糊帯の高さ・縮みの積算が完全一致、折れ角は符号反転 |
| **途中形状でも接着点がつながる** | 隣接する紙 (i, i+1) の糊帯上の点の距離 < 紙厚×3（半径 3 箇所 × 両パネル） |
| **紙が伸び縮みしない** | 頂点から求めた螺旋の弧長が、平らなときの長さと 9 桁一致。半径も一致。直線距離が平らな長さを超えない |
| **セルの隙間が本当に空間として存在する** | 隣接する紙の距離が展開量とともに単調に増え、全開で 4 mm 超 |
| **紙が貫通しない** | すべての隣接ペアで、紙面に垂直な向きの間隔が 0.4×紙厚 以上、かつセル最大幅以下 |
| 閉じ切りで平ら／全開で頂点は不変・外周は沈む | 全行の半数以上が 2 mm 以上沈む |
| NaN・退化した法線が出ない | 41 段階の展開量で全頂点・全法線を走査 |
| 底の縁は常に机の高さ（接地する） | 全展開量で y = 0 |
| 毎フレームの再生成をしない | 同じ展開量なら `Panel.update` が false を返す |

`tests/unit/store.test.ts`（10 件）は、止まる／逆転する／継ぎ足せる、
**クリック回数や経過時間だけでは完成しない**、弾性戻りが上限を超えない、
全開でだけ留められる、触れただけでは留まらない、
留める→外す→閉じる→開く を 30 周しても状態が壊れない、を確かめる。

### ブラウザ操作テスト（Playwright, 35 件成功 / 0 失敗 / 9 skip）

4 つの CSS viewport すべて（390×844 / 844×390 / 820×1180 / 1180×820）で実行。
skip は「1 構成で足りる検査」として意図的に 1 構成に絞ったもの（資源の単調増加・色選び・操作記録）。

| 検査 | 結果 |
| --- | --- |
| 起動直後に作業台から始まり、タイトルメニューを挟まない | 4/4 成功 |
| ドラッグの停止・逆転・再開に追従する（記録した指の位置と展開量の向きが毎区間一致） | 4/4 成功 |
| タップの回数や経過時間だけでは完成しない | 4/4 成功 |
| つかみ代を直接つまんでも開く（中心から 27 px ずれても掴める） | 4/4 成功 |
| 全開 → 留める → 外す → 閉じる → 開く を 2 周 | 4/4 成功 |
| 途中で画面を回しても進捗を失わず、回した先でも操作できる | 4/4 成功 |
| 二本目の指・pointercancel の途中割り込みで壊れない | 4/4 成功 |
| 繰り返し 3 周しても入力が止まらず、geometry / texture / program が増えない | 成功（phone-portrait） |
| 完成後に紙見本で色を選べ、外せばまた開閉できる | 成功（phone-portrait） |

### 画面証拠（`artifacts/evidence/`）

4 viewport × 4 状態 = 16 枚。すべて品質を落とさない設定（`?fixq=1`）で撮影。

- `*-1-closed.png` 閉状態（平たい紙束が机に立っている）
- `*-2-partial.png` 途中（扇が開き、セルの隙間と影が見えている）
- `*-3-open.png` 全開
- `*-4-clipped.png` 留めたあと（カメラが少し引き、完成したツリーと元の平たい材料が同じ机上に並ぶ）

追従性の記録:

- `artifacts/evidence/phone-portrait-drag.webm` — ゆっくり引く → 止める → 戻す の実操作の動画
- `artifacts/evidence/phone-portrait-trace.json` — 同じ操作の毎フレームの記録（時刻・展開量・指のX・ドラッグ中か）

開発用視点（`artifacts/dev-views/`, `?dev=1`）: 展開量 0 / 0.5 / 1 × 前・側・背・上 の 12 枚。
- `t0-side.png` 閉状態を真横から。厚さ約 2.6 mm の板として机に接地している。
- `t0.5-back.png` 半開を背面から。開いた側のセルの実空間と、まだ閉じている側が同じ紙で連続している。
- `t1-top.png` 全開を真上から。24 枚が 360° を一周ぶん覆い、継ぎ目が 1 本だけ見える。

### 描画の実測値（`artifacts/evidence/*-stats.json`）

| viewport | 三角形 | draw call |
| --- | --- | --- |
| 390×844 | 66,436 → 66,472 | 15 → 18 |
| 844×390 | 66,764 → 66,556 | 18 → 20 |
| 820×1180 | 66,436 → 66,472 | 15 → 18 |
| 1180×820 | 66,448 → 66,484 | 16 → 19 |

初期予算（可視三角形 15 万以内 / draw call 80 以内）に対して、三角形は約 44%、draw call は約 25%。
影を落とす主要光源は 1 つ（窓光）。他の 2 灯（手前の返し光・作業灯）は影を落とさない。
DPR の上限は 1.5。

## 7. 残る制約・未確認

### 未確認（やっていないことを、やったことのように書かない）

- **実機 Safari（iPhone / iPad）は未確認。** 本コンテナに実機も iOS シミュレータも無い。
  検査はすべて Chromium（SwiftShader）で行っており、これは **WebKit エミュレーションですらない**。
  実機 Safari 固有の挙動（WebGL2 の細部、`touch-action` と Safari のスクロール、
  AudioContext の再開規則、`env(safe-area-inset-*)`、ホームバー領域）は未検証。
- **WebKit エンジンでの実行も未確認。** Playwright の webkit ビルドは本環境に無い。
- **性能は未確認。** SwiftShader では全品質で約 1.1 fps しか出ない。これはソフトウェア
  ラスタライザの数値であって、実機 GPU の予測にはならない。
  「30 fps を安定した下限」という目標は**達成を確認していない**。
  段階的な品質低下（背景 → 影解像度 → 描画解像度）は実装済みだが、実機で発火する
  閾値は未調整。
- **4歳児による理解・面白さの検証は未実施。** 対象年齢の利用者による試遊は行っていない。
  「一度触るだけで動く」「20〜30 秒で文字なしに看板操作を経験できる」は設計意図であって、
  観察された事実ではない。
- **参照資料 2 ページは取得できなかった**（本作業環境の送出ポリシー）。
  構造は課題文の記述だけを根拠にした再構成であり、現物の観察に基づく寸法は 1 つも無い。

### 作中の簡略化（意図して現実と変えたところ）

1. **端台紙のたわみ**: 0.8 mm の板紙を、紙と同じ縦の縮み `Y(u,y)` に従わせている。
   剛体の平面にすると外周側で最大 12 mm の食い違いが出て接着が破れるため、
   接着の整合を優先した。実物の板紙はここまでたわまない。
2. **芯（軸まわり）**: 半径 0.306 mm より内側は穴にしてある。実物はここで紙が
   ねじれて詰まる。角度の間隔がちょうど紙厚になる半径で切っている。
3. **紙 1 枚の切断端**: 幅 0.04 mm のリム帯として実体を持たせてあるが、画面上は
   0.1 px 未満で見えない。見た目の厚みは 24 枚の並びと 0.8 mm の端台紙が担う。
4. **糊帯の紙厚**: 貼られた 2 枚を常にちょうど紙 1 枚ぶん離すため、展開量に比例する
   ±0.02 mm の法線方向のずれを頂点に持たせている（閉じ切り付近の面重なり対策）。
5. **半径依存の縮みと板紙**: 縮み `s(u)` は「半径一定の材料線」に沿って厳密だが、
   実物の紙は芯の近くで細かくしわを作る。そのしわは作っていない。
6. **窓の外**: 空と遠くの木立を描いた 1 枚の板。室内の光は方向光 1 灯で代表させている。

### 既知の粗さ

- 手前のドラッグ帯と紙見本・クリップが同じ帯に同居するため、
  「触った位置」ではなく「12 px 以上動いたか」で色選びと開閉を切り分けている。
  指が震える利用者では意図しない開閉が起きうる。
- 横画面は縦画面より机の手前が狭く、クリップの当たり領域が画面下端から 40 px の位置になる。
  これより低い縦横比では見切れる可能性がある。
- 段の境目の丸め（1.5 mm）は輪郭の退化を避けるための便宜で、裁断の実際とは関係しない。
