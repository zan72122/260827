{
  "name": "kurukuru-flower-birthday",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "くるくる、お花のバースデー — a one-finger WebGL cake decorating game for young children",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/three": "^0.185.4",
    "playwright": "^1.62.1",
    "typescript": "^5.9.3",
    "vite": "^8.2.2"
  }
}
