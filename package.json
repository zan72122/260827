{
  "name": "chiisana-ten-no-sono-naka-e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A one-swipe magnification dive from a glass slide to the cellular level of normal skin (H&E).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview --port 4173 --strictPort",
    "capture": "node tools/capture.mjs",
    "qa": "node tools/qa.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "playwright": "^1.49.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
