# REFERENCE_NOTES

制作の寸法と手順が、どこから来たのかを分けて記録します。
「確認できた資料」「本作品の設計上の仮定」「未確認」を混ぜないための文書です。

---

## 1. 参照を指示された資料と、その閲覧状況

| 資料 | URL | 本セッションでの閲覧 |
| --- | --- | --- |
| Wilton "How to Pipe Buttercream Roses" | `https://blog.wilton.com/buttercream-roses/` | **閲覧できていない（未確認）** |
| Wilton "How to Use a Flower Nail" | `https://wilton.com/how-to-use-a-flower-nail/wltech-7854/` | **閲覧できていない（未確認）** |

この開発環境の外向き通信は egress プロキシで制限されており、上記 2 件はいずれも
`EGRESS_BLOCKED`（`blog.wilton.com` / `wilton.com` へのアクセスがポリシーで拒否）で
取得できませんでした。プロキシ状態は `curl -sS "$HTTPS_PROXY/__agentproxy/status"` で
確認しています。**したがって、これらの資料の内容・寸法・写真は一切参照していません。**
資料に基づいて確認した、とは書けないため、以下の工程・寸法はすべて
「一般知識に基づく設計上の仮定」として扱ってください。

資料画像そのものは閲覧しておらず、当然ながらゲーム素材へ複製・再配布もしていません。
本作のテクスチャ・形状・音はすべてコード内で手続き的に生成しています
（`src/render/textures.ts`, `src/audio/AudioKit.ts`, `src/build/*`）。

---

## 2. 明示的に与えられた設計寸法（指示書由来）

指示書に「制作開始時の採用寸法」として与えられた数値です。
本作品の設計寸法であり、実測値ではありません。

| 部位 | 値 | 実装箇所 |
| --- | --- | --- |
| ケーキ直径 | 180 mm | `DIM.cakeRadius = 90 mm` |
| ケーキ高さ | 約 80 mm | `DIM.cakeHeight = 80 mm` |
| 回転台直径 | 約 280 mm | `DIM.turntableRadius = 140 mm` |
| フラワーネイル円盤直径 | 約 38 mm | `DIM.nailDiscRadius = 19 mm` |
| 花の幅 | 約 30〜40 mm | 大輪の実測（後述）で 33〜37 mm |

ワールド単位は **1 単位 = 1 メートル** に統一しています（`src/core/units.ts`）。
ソース上は `mm(...)` で書き、内部でメートルへ変換します。

---

## 3. 設計上の仮定（資料未確認。妥当と思われる値を置いたもの）

いずれも実測ではありません。実物と照合していません。

| 部位 | 採用値 | 仮定の根拠 |
| --- | --- | --- |
| 花びら口金の全長 | 34 mm | 一般的な小型口金の見た目に合わせた仮定 |
| 口金 後端の半径 | 11 mm | 同上 |
| 口金の板厚 | 0.32 mm | 薄板ステンレスとして妥当な範囲の仮定 |
| 口金スロット長 | 11.5 mm | 花びらの幅を 30〜40 mm に収めるための逆算 |
| スロット開口（広い側 / 狭い側） | 4.4 mm / 0.55 mm | 花びらが「根元は厚く縁は薄い」帯になるための仮定 |
| ネイル軸長 / 軸半径 | 72 mm / 1.6 mm | 手で保持できる寸法としての仮定 |
| 紙片 | 45 mm 角 / 厚さ 0.14 mm | ネイル円盤 38 mm を覆う最小サイズからの仮定 |
| 皿 | 直径 200 mm / 高さ 18 mm | 一般的な取り皿としての仮定 |
| ろうそく | 直径 6.4 mm / 高さ 62 mm | 一般的な誕生日用としての仮定 |
| 回転台 台座高 | 34 mm | 作業台上の見た目からの仮定 |
| スポンジ／フィリングの層構成 | 3 層 + 2 層 | 断面が「層」として読めるための仮定 |
| 側面クリーム被膜の厚み | 3 mm | 断面に薄い帯として見えるための仮定 |

---

## 4. 工程モデル（一般知識に基づく仮定。資料照合なし）

実装している手順は次の通りです。**資料で裏付けたものではありません。**

1. ネイルの円盤に少量のクリームを置き、紙片を貼る。
   → 本作では開始時点で紙片が載っている状態から始めます。
2. 丸口金でクリームの芯（円錐）を絞る。
   → 本作では開始時点で芯が立っている状態から始めます（`FlowerBuilder.buildCone`）。
3. 花びら口金を、**広い開口を下（芯の側）** にして構える。
4. 片手でネイルの軸を持って回し、もう片手で袋を支えて絞る。
5. 内側の列（3 枚）は袋を高く構え、帯が芯へかぶさるように。
6. 外側の列（5 枚）は袋を寝かせ、帯がほぼ垂直に立つように。
7. 完成後、紙ごと／またはリフターで底から支えてケーキへ移す。

本作では 3〜7 を操作対象にしています。1〜2 は開始状態です。

### 口金の姿勢モデル（`src/flower/petalPath.ts`）

口金の口は袋の軸に対して垂直なので、**袋を水平からどれだけ持ち上げるか**
（`lean`）が決まれば、スロットの向きは一意に決まります。

- `slot = up·cos(lean) − radialOut·sin(lean)`
- `body = radialOut·cos(lean) + up·sin(lean)`（口 → 袋の向き）

内側の列は `lean ≒ 0.55 rad`（約 32°）、外側の列は `lean ≒ 0.16 rad`（約 9°）。
「花びらが外へ開く」表現は、帯全体を外へ倒すのではなく、
**自由縁の反り（`furl`）** で表現しています。実物のバタークリームでも
開くのは帯の縁であり、根元は芯に接したままです。

---

## 5. 花の実寸（本実装の出力）

`ROWS`（`src/flower/FlowerBuilder.ts`）の値から計算した設計値です。

| 項目 | 小輪（内側 3 枚のみ） | 大輪（+ 外側 5 枚） |
| --- | --- | --- |
| 幅 | 約 20 mm | 約 33〜37 mm |
| 高さ | 約 15 mm | 約 17 mm |
| 花びら 1 枚の帯幅（最大） | 13.5 mm | 14.0 mm |
| 花びら根元の厚み | 2.2 mm | 2.5 mm |
| 自由縁の厚み | 0.32 mm（`EDGE_MIN`） | 同左 |

「花の幅 30〜40 mm」という指示に対し、大輪が該当します。
小輪はつぼみに相当し、意図的に小さくしています。

---

## 6. 実装で使っていないもの

- 資料の画像・図版（閲覧していないため、当然使用していません）
- 外部の 3D モデル、テクスチャ、フォント、音源
- 実行時の外部 API・CDN・画像ホットリンク

テクスチャは `src/render/textures.ts` が起動時に Canvas 2D で生成、
音は `src/audio/AudioKit.ts` が Web Audio のオシレータとノイズから合成しています。
帰属表示が必要な第三者素材はありません。
